# requirements.txt — the pinned environment (TE §8.1, §13.1; TC-06; TA-02).
#
# Purpose: the single governed pin surface. Every run's environment lock records
#   the SHA-256 of this file plus a per-run `pip freeze` (TE §13.1, REQ-ENG-10).
# Inputs: none — consumed by `pip install -r requirements.txt` on Python 3.11
#   (the governed interpreter pin, TS-01/TC-03d).
# Re-run behaviour: installing from these pins on a clean Python 3.11 must be
#   reproducible on both governed platforms (Kaggle and local, TE §9.1).
#
# TensorFlow is PINNED below by the project decision owner's ruling of 2026-09-10
# ADOPTED as **D-36** (2026-09-10); prepared at
# governance/CHANGE_RECORD_2026-09-10_owner_rulings_implementation.md and recorded in
# evidence/DECISIONS.md. It supersedes the earlier state, recorded
# here as history: the pin was `TBD — freeze gate` under code-generation Q3=A and no
# implementer could fill it by convenience (Vision §1.2; TE §1.1) — the owner did.
# PyTorch is prohibited outright (TE §8.3). No GPU package: CPU is a complete
# execution path (TC-01), never a dependency of any result.

# --- scientific base (CPU only) -------------------------------------------------------
numpy==1.26.4
pandas==2.1.4
pyyaml==6.0.1
# scikit-learn: M-04 Ridge and M-05 Random Forest (TS-M-03). An ENGINEERING pin on
# foundation's precedent, added under models-and-baselines code-generation Q3=A and
# recorded in governance/CHANGE_RECORD_2026-09-06_BLK03_confirmatory_contract.md.
# Imported lazily inside the fit paths; absence refuses by name. Its CV splitters are
# never used (fold construction is features-and-splits').
scikit-learn==1.4.2
# tensorflow: the ONE neural stack (TE §8.3; PyTorch prohibited). Version selected by the
# project decision owner's ruling of 2026-09-10, adopted as D-36 — the CPU wheel, not a GPU
# build (TC-01), and the version src/models/lstm.py was implemented against (TE §8.1's
# candidate 2.21.0). NOT YET VERIFIED: PyPI is unreachable from the implementation
# environment, so installation, import, API-compatibility and the TE §8.1 both-platform
# (Kaggle AND local) check are OWED — freezing the pin makes M-06's guard pass; it does
# not make the environment exist.
tensorflow==2.21.0

# --- figures (TE §8.1, Required) ------------------------------------------------------
# matplotlib: the ONLY third-party package that EXECUTES in src/ and is absent from this
# file. `src/evaluation/plots.py:253-254` imports it lazily, and its own refusal message at
# :249 names "requirements.txt, the pin surface" — an assertion that was FALSE until this
# block existed (Recommendation 38). `pyproject.toml:21` keeps `dependencies = []`
# deliberately, so this file is the single pin surface, and a package outside it is outside
# TE §13.1's environment lock.
#
# THE VERSION IS NOT WRITTEN HERE, AND THAT IS DELIBERATE.
#   value: TBD — freeze gate
#   owner: Student (CR-2026-09-20-GOV-CG-01-DISPOSITIONS §5 item 15, due before G-07)
# That disposition makes fixing the version a Student act "if no repository evidence
# determines it". It does not. Derived 2026-09-20, printed before assertion:
#   * `grep -rnoE "matplotlib[=<>~ ]*[0-9]+\.[0-9]+(\.[0-9]+)?"` over the entire working
#     tree excluding `.git/` → 0 matches. No version literal exists anywhere.
#   * matplotlib appears in all three environment captures on disk
#     (artifacts/exec_evidence/environment.json,
#     evidence/audit_gfz_2026-09-18/environment_supplemental.json,
#     evidence/b01_tolerance_basis_2026-09-20/environment.json) → 0 times. No `pip freeze`
#     has ever recorded it.
#   * TE §8.1's table lists matplotlib "Required" with NO version, unlike the TensorFlow
#     row, which carried the 2.21.0 candidate that became D-36.
# An implementer may not fill a `TBD — freeze gate` value by convenience (Vision §1.2;
# TE §1.1), and guessing one here would place an INVENTED figure inside the very artifact
# whose SHA-256 is the environment lock. The requirement is therefore carried as a comment,
# the same mechanism the gitleaks pin below already uses: the value stays inside the lock
# hash while `pip install -r requirements.txt` stays runnable.
#
# TO CLOSE: the Student records the version the figures were rendered against, replaces
# this block with a bare `matplotlib==<version>` line, regenerates the environment-lock
# hash, and confirms plots.py:249's message has become accurate.
#   matplotlib==TBD — freeze gate   (← replace with the real version; do NOT guess)

# --- test suite (a first-class deliverable, TC-06) ------------------------------------
pytest==8.2.2

# --- lint + format (team.md § Code Style, Q10=A) --------------------------------------
ruff==0.4.8

# --- secret scanning ------------------------------------------------------------------
# gitleaks is a Go binary, not a pip package; its pin lives here as a comment so
# the environment lock's requirements hash covers it, and is asserted by
# scripts/gate_secret_scan.py and .githooks/pre-commit (SD-01, Q2=A):
#   gitleaks 8.18.4 (pinned; upgrade requires a reviewed change, not a drift)

# Stage Diary — nfr-requirements

## Interpretations

- 2026-08-31T00:00:00Z — Produced only `security-requirements.md` and `tech-stack-decisions.md`; context: the directive's `produces` list carries exactly those two, because `foundation` is `kind: library` and the stage's `produces_kinds` restricts `performance-requirements`, `scalability-requirements` and `reliability-requirements` to `[service]`/`[service, ui]`. Writing the other three would have invented applicability the stage frontmatter denies.
- 2026-08-31T00:00:00Z — Read the TensorFlow pin as a `TBD — freeze gate` rather than a decision; context: TE §8.1 names 2.21.0 as "the current Python 3.11-compatible candidate" and states "the exact compatible pin is frozen only after Kaggle/local fixture installation passes". Neither fixture has run, so per TE §18.2 recording 2.21.0 as the pin would be filling a freeze-gate value by convenience.

## Deviations

- 2026-08-31T00:00:00Z — Did not restate a quantified availability or latency target for this unit; context: Step 3 lists Performance/Scalability/Reliability as categories to assess, but the per-kind applicability in `produces_kinds` excludes their artifacts for a `library` unit. The assessment is recorded here and in the security artifact's scope note rather than in artifacts the stage does not produce for this kind.

## Tradeoffs

- 2026-08-31T00:00:00Z — Chose to carry `foundation`'s existing open items (the `dataset_version` verify-on-write read-back population, the `IntegrityError` module home, Kaggle durability semantics) as stated dependencies rather than re-asking them; context: all three are already routed to the project decision owner by the approved `functional-design`, and re-asking would create a second, competing route to the same ruling. Only the two decisions genuinely in this stage's lane — the TA-22 scan scope and the credential provisioning mechanism — were put to the human.

## Open questions

- 2026-08-31T00:00:00Z — `foundation`'s own `business-logic-model.md` lead box and its `§ Assumptions` G-09 bullet both read "module creation is authorised" while W-9's barred list and the BLK-01 note state creation stays gated; context: this is the same self-contradiction class swept out of `fixtures-and-reproducibility` on 2026-08-30, and it survives in an approved `functional-design` artifact this stage consumes. Not corrected here — editing a completed stage's artifact is outside this stage's produces — and surfaced to the human instead.

- 2026-08-31T00:00:00Z — The summary confirmation had to be recorded twice; context: the first `aidlc-log.ts decision`/`answer` pair omitted `--checkpoint summary-confirmation` and `--unit foundation`, so the engine saw no unit-scoped receipt and refused completion. The second pair carried both flags and a fresh human turn between them. Consequence worth remembering: artifacts written before the receipted confirmation do not satisfy the completion precondition, which is why `tech-stack-decisions.md` needed a native-tool write afterwards.

- 2026-08-31T00:00:00Z — Corrected four live sites in `governance-guards`' approved `functional-design` on the owner's explicit annotate-in-place approval; context: `business-logic-model.md` W-10's mechanism sentence and its "four modules" restatement, and `business-rules.md` R-28's own Rule statement plus the 2026-08-28 ruling box's "no fourth", all still asserted the superseded exempt-module count while `domain-entities.md` § 10 and R-28's own box twenty lines below already carried five/six. Found while reading the unit's artifacts as this stage's `consumes`, not by a review. Root cause is the 2026-08-29 repair sweeping only the five sites its finding enumerated — `project.md` `fd-2026-08-30-sweep-derive-sites`. Superseded figures preserved in place; a change record under `governance/` may be owed and is the owner's to file.

- 2026-09-01T00:00:00Z — Reviewer dispatches hit a session rate limit at `features-and-splits` (resets 12:30am Asia/Tehran); context: marked that unit's request `--retry-pending` so re-running costs no iteration, then continued authoring `models-and-baselines` without a reviewer and opened its request too. Two units now carry unmatched `REVIEW_REQUESTED` rows awaiting dispatch. Authoring and reviewing are separable in this stage because the reviewer reads only finished artifacts, so the batch can be run once the limit clears.
- 2026-09-01T00:00:00Z — Every unit so far has yielded at least one requirement the design states but never tests, and two yielded a mechanism that cannot run at all; context: `acquisition` had no retrieval-resilience rule of any kind, `target-standardization`'s closed-set criterion is unsatisfiable while "documented QC" is undefined, and `models-and-baselines` carries 7 of 9 requirements with no acceptance row. The pattern is consistent enough to be worth stating at the gate as a stage-level finding rather than twelve separate ones.

## Deviations

- 2026-09-01T00:00:00Z — Derived each unit's requirement set by grepping that unit's own `functional-design` files for requirement IDs, rather than set-differencing `requirements.md` against the artifacts; context: the stage prose does not prescribe a method, and the per-unit coverage table looked authoritative. It is not. On `statistical-inference` the grep-based check came back clean on both FR families — the reviewer independently confirmed the first clean FR result in five units — and still missed **NFR-REP-01**, whose substance both artifacts were built on while citing TE §13.7 directly and never the NFR ID. A unit's design naming an ID is a different set from the requirements a unit's artifacts state obligations against.

## Tradeoffs

- 2026-09-01T00:00:00Z — Chose to keep authoring the remaining units while reviews ran in the background rather than serialising author-then-review per unit; context: the reviewer reads only finished artifacts, so the two are separable, and a session rate limit had already idled one dispatch. The cost is that a defect class found on unit N was sometimes not yet known when unit N+1 was written — the requirement-coverage completeness defect recurred on five consecutive units partly for this reason.

- 2026-09-01T00:00:00Z — The requirement-coverage completeness check found a defect on **seven of twelve units**, from three distinct causes, and twice a repair I made introduced a fresh instance of the defect it was fixing; context: I built each coverage table from the unit's own `functional-design` map, then from a grep of its design files, and neither is set-differencing `requirements.md`. The rule that would have prevented all seven: **a unit's coverage table must cover every requirement whose text its artifacts reproduce, whoever owns the acceptance row** — ownership is not the filter, reproduction is. Two of the seven were requirements carrying **approved acceptance rows** (TA-34, TA-35) that appeared nowhere in my artifacts.
- 2026-09-01T00:00:00Z — Two repairs created fresh defects of the same class they were fixing; context: on `evaluation-and-comparison` a dependent count in the sibling artifact was left stale through two corrections until it was arithmetically wrong, and on `regimes-diagnostics-reporting` I bumped a headline 17 → 19 and left its own decomposition one line below reading 11 + 6 = 17. `project.md` already records both patterns (`delivery-planning:dp-1`, `fd-2026-08-30-sweep-derive-sites`); knowing the rule did not prevent committing it, which suggests the fix is a mechanical re-derivation step after every count edit rather than more care.

- 2026-09-01T00:00:00Z — The requirement-coverage defect ultimately touched **all twelve units**, not the seven recorded above; context: the post-rejection confirming passes found five more — `governance-guards` (NFR-AUD-01's registry behaviour stated, ID never cited), `acquisition` (NFR-DQ-01's acceptance row blank when TA-19 exists, and a "2 with no acceptance row" figure right only by coincidence because it counted the map rather than the table), `external-products` (FR-P1-04-1, the source row for the allowlist-not-denylist framing, reproduced on both limbs while only NFR-IRI-01 was cited against the identical rows), `features-and-splits` (NFR-TDEF-01 and FR-P1-03-3 absent while the unit builds a dataset and produces masks — two of the four classes that rule requires stamped), and `models-and-baselines` (NFR-PHASE-01's protected-hash refusal stated in a stack decision, ID uncited). The last was found by my own pre-dispatch sweep rather than by a reviewer. A sixth pattern also appeared twice: an acceptance-row list truncated to its first row when the requirement carries two (NFR-AUD-01 → TA-10, TA-21).

- 2026-09-01T00:00:00Z — A silent gap in an ID sequence is a defect even when the exclusion is correct; context: on `inventory-and-registry` the reviewer raised FR-P1-02-6 as an uncited Major, but its own evidence — zero hits for the requirement's characteristic terms — showed the artifacts depend on the custody FR-P1-02-6 establishes without reproducing its text, and `functional-design` had already fixed the unit's set as {-1,-2,-3,-4,-5,-7,-8} by set difference, with `governance-guards` carrying -6. Adding a row would have contradicted an adjudicated upstream set. The real defect was that the row list ran 1,2,3,4,5,7,8 with no stated reason for the gap. Resolution: state the exclusion and why, rather than either adding the row or leaving the skip silent.

## Open questions

- 2026-09-01T00:00:00Z — Two reviewer findings this round were wrong on the facts, and both would have caused a bad edit if applied unverified: `external-products`' iteration-2 pass reported the both-limbs IRI residual as still confined to an `## Assumptions` bullet when it had been moved into SEC-E-01's rule body on 2026-08-31 (the bullet was deliberately retained alongside it), and `acquisition`'s pass reported two standing Minors as unresolved when both were fixed the same day — the finding rows remain in the table by this project's preserve-don't-overwrite convention, which reads as "still open" to a reviewer that only sees the table. Open question for the gate: whether preserved finding rows need an explicit resolved-on marker, since the convention that keeps the audit trail is actively misleading the next reviewer.

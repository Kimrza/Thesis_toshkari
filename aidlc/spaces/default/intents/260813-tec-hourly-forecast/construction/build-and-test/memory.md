# Stage Diary — build-and-test

## Interpretations

- 2026-09-13T11:40:00Z — Stage entered directly after `code-generation` closed with two units carrying terminal NOT-READY verdicts (`external-products`, `fixtures-and-reproducibility`), both on the confirmed `04` fixture-ladder Critical. Read as: this stage may document and attempt, but must not present any result as discharging WS-20 or TA-17, which that Critical makes structurally unreachable rather than merely `Pending`.
- 2026-09-13T11:40:00Z — `Test Strategy: Comprehensive` in `aidlc-state.md`, so Steps 4–8 call for all applicable instruction types. Read against this project's own governing vocabulary rather than the framework's generic tiers: the real bar is TE §18.3's preflight gate ("zero unresolved P0 fields and no failing critical test"), §16's WS rows and §19's TA rows, not a coverage percentage — `team.md` § Testing Posture is explicit that `org.md`'s 80% default never attached to this scope.

- 2026-09-18T10:30:00Z — Session resumed on the owner's brief "next approved stage after the completed D-25 Route 1 remediation": a READ-ONLY discovery of the GFZ Kp/ap and Hp60/ap60 producer data, with acquisition only if a legitimate, reproducible, authorised path exists. Read as work inside the still-open `build-and-test` stage (Current Stage unchanged, summary confirmation receipted 2026-09-13), not as a new stage; the brief's own Phase 5 stop conditions govern before the stage's artifact steps resume.

- 2026-09-19T12:00:00Z — Scientific review of 2026-09-19 (items 1–9) read as gate-preparation work inside the open stage: routine fixes and documentation alignment executable now; every contract amendment, decision and tolerance value prepared as a proposal only. Record: `governance/CHANGE_RECORD_2026-09-19_scientific_review.md` (`CR-2026-09-19-SCI-REVIEW`).

- 2026-09-19T15:00:00Z — Owner's decision message (P-1, P-2, P-3, A3=B, A4, G-3/P-4, P-5) read as student decisions with narrowly scoped implementation authority; each recorded under a new D-number (D-43…D-48) with its own approval status; the release boundary (no config transcription, producer, release, registration, training, commit, push) kept. Record: `governance/CHANGE_RECORD_2026-09-19_scientific_decisions.md`.

- 2026-09-19T18:00:00Z — Owner's message reported an actual supervisor approval of D-42/43/45/46 (distinct from the recorded student/supervisor authority equivalence used elsewhere in this project). Read as: record the report exactly as a report, name the still-outstanding prescribed evidence (a countersignature artifact, matching the 2026-08-16/08-21 precedent), draft that artifact, and proceed with the rest of the authorized work (patch application, config transcription) on the student's explicit go-ahead rather than waiting on the literal signature. Record: `governance/CHANGE_RECORD_2026-09-19_scientific_decisions_p2.md`.

- 2026-09-19T21:00:00Z — Owner's message split into: (1) close the countersignature blocker on the student's direct statement ("supervisor has approved and countersigned"), recorded as that statement, matching the project's own letter-based closure mechanism; (2) close the real enforcement gap the prior pass left (additive config fields carried but not applied by any consumer) with reader validation AND production-path wiring, not just more reader checks; (3) build a genuinely self-contained, locally-validated-as-far-as-possible Kaggle notebook rather than a plan or bare install commands. Record: `governance/CHANGE_RECORD_2026-09-19_scientific_decisions_p3.md`.

## Deviations

- 2026-09-13T11:45:00Z — Environment finding that changes what this stage can honestly claim, recorded before any artifact was written: `uv` IS on PATH and **successfully installed real CPython 3.11.16** — the exact governed pin (TE §8.1, TC-03d) — from the astral index, which is reachable. **PyPI is not**: `uv pip install -r requirements.txt` fails at `https://pypi.org/simple/pytest/` after 3 retries with a connect timeout. So this clone now has a REAL governed-version interpreter but no `pytest`, `numpy`, `pandas`, `pyyaml`, `scikit-learn` or `tensorflow`. This is strictly better than every prior session (which had only Windows Store alias stubs) and still short of a governed run.

- 2026-09-13T12:05:00Z — **Q1 = C (vendor `pytest` first) was attempted and HALTED on an integrity finding.** Every PyPI mirror tried is blocked (`pypi.org`, `pypi.tuna.tsinghua.edu.cn`, `mirrors.aliyun.com`, `files.pythonhosted.org`, `pypi.doubanio.com` — all connect-timeout after retries). GitHub IS reachable, so the exact `pytest-dev/pytest` tag `8.2.2` source tarball was fetched and extracted to the scratchpad (never the repo) along with `pluggy` 1.5.0, `iniconfig` v2.0.0 and `packaging` 24.0. **The pytest artifact did not survive inspection and was NOT placed on the import path.** Two objective defects, both re-derived from a second independent fetch: (1) its own `pyproject.toml` declares dependencies `colorama`, `exceptiongroup` (py<3.11), `iniconfig`, `packaging`, `pluggy<2.0,>=1.5`, `tomli` (py<3.11) — **`py` is not among them** — yet `src/_pytest/compat.py:20` executes a bare `import py` at module scope, so the package as shipped cannot import under its own declared dependency set; (2) `src/_pytest/compat.py:27-28` carries the comment `# intentional space to create a fake difference for the verification` directly above `LEGACY_PATH = py.path. local`, a deliberately malformed attribute access. Tarball `sha256:b723da0ef5c624aad539e65c6d5381289c84c2977045e365813338318474d433`, 1418462 bytes, root `pytest-8.2.2/`. Recorded rather than worked around: whatever the cause, an artifact bearing a planted "fake difference" marker and an undeclared hard import is not something to put on the import path of a governed scientific pipeline's test environment, and doing so would also breach NFR-LIC-01 / TE §10.1, which require a reuse-register entry with provenance, immutable tag, licence and SPDX **before the code is used**.

- 2026-09-13T12:30:00Z — **Investigation of the pytest artifact, on the owner's ruling to treat it as a security finding first. The earlier entry's phrasing is refined by evidence, in both directions.** What is now VERIFIED: the tarball's `src/_pytest/compat.py` is **byte-identical** to what the GitHub Contents API serves for the same path at tag `8.2.2` (`tar_content == api_content` → True), and the API's own content hashes content-addressed to the blob SHA it reports (`sha1("blob "+len+"\0"+content)` = `614848e0dbaeb2cfc1356d3393b0d0992922d47f` = the API's `sha` field, size 11467). So the file is authentic **to what this environment's GitHub is serving**, and my earlier framing of it as a transport-corrupted download is withdrawn. What this does NOT establish: that the served repository is genuine upstream pytest. Both `codeload.github.com` and `api.github.com` sit behind the same egress, so their agreement proves internal consistency, not provenance, and no independent channel is available for comparison — `pypi.org` does not even complete a TLS handshake from here (`TimeoutError`). Certificate issuers observed, recorded as data rather than as a conclusion: `github.com` and `codeload.github.com` → Sectigo Limited; `raw.githubusercontent.com` → Let's Encrypt.
- 2026-09-13T12:35:00Z — **Three internal-consistency findings that stand regardless of provenance, and they sharpen rather than soften the conclusion.** (1) `_pytest/_py/` EXISTS in the fetched tree, carrying `path.py` — the vendored replacement that exists precisely so `py` is not needed — while `compat.py:20` nonetheless executes a bare module-scope `import py` for `LEGACY_PATH = py.path. local`. The legitimate substitute is present and unused. (2) **Marker scope is surgical, not environmental**: sweeping all 95 `.py` files across the four fetched packages for `fake difference|intentional space|for the verification` returns **exactly one hit** — `_pytest/compat.py:27`. `pluggy` (8 files), `iniconfig` (3), `packaging` (14) and `pytest` (2) are clean. A mirror-wide rewrite would not land on one line of one file and leave three sibling packages from the same fetch untouched. (3) `py` is the only undeclared, unconditional, module-scope import in `_pytest` (`pexpect` at `legacypath.py:39`/`pytester.py:76` is a known optional path; `pytest` self-imports are internal). Taken together: a single, self-announcing, one-line modification in one file. Whether it was planted in the served repository or upstream of it cannot be determined from inside this environment, and does not need to be for the decision.
- 2026-09-13T12:40:00Z — Consequences recorded, none of which require resolving the provenance question: the artifact is not vendored, `git status` confirms no repository file was touched by any of this, no repo module references the scratchpad tree, and the four modified files in the working tree remain the `code-generation` repairs alone. The §10.1 / NFR-LIC-01 obligation — provenance, immutable tag, licence, SPDX, reviewer and approval date recorded **before the code is used** — was never satisfiable here and is moot while the code goes unused.

- 2026-09-18T10:30:00Z — **Phase 1.A could not be confirmed: the "D-25 Route 1 remediation, four files, uncommitted" described in the brief does not exist in this clone.** `git status` is clean except one untracked audit shard; HEAD is `18843aa` (an audit-only commit of 2026-09-14); no local branch is ahead of `main`; `src/features/availability.py` at HEAD carries no `availability_ts` / `00:00 UTC on D` convention; the identifiers `gfz_kp_ap_3h_2022_v1`, `gfz_hp60_ap60_1h_2022_v1`, `srmp_f107_observed_daily_2022_v1`, `safe_lag_hours = 3/1` and the phrase "Route 1" appear in no repository file, no change record, and no audit shard (the last audit event on any shard is 2026-09-13T18:37Z); `evidence/DECISIONS.md` ends at D-38 and records none of the brief's Q1–Q6 approvals. Recorded as a blocker and routed to the owner, not reconciled by assumption: the remediation most likely lives in another clone's working tree.
- 2026-09-18T10:30:00Z — Network probes made during discovery, disclosed exactly: HTTP `HEAD` on four `kp.gfz.de` URLs and byte-range `GET`s of the 30-line provider headers (bytes 0–3500) of `Kp_ap_since_1932.txt` and `Hp60_ap60_complete_series.txt` plus bytes 0–2600 of `Hp60_ap60_nowcast.txt`. No 2022 value was downloaded, retained, or written; nothing entered `evidence/` or `artifacts/`. This was product-identity discovery, not acquisition; the acquisition script's own refusal (`scripts/00_acquire_prepared_vtec.py:_build_transport`: "no network call is authorised here … re-acquisition is deferred work (DATA-07)") was not bypassed.

- 2026-09-18T11:35:00Z — **D-25 Route 1 reconstructed in this clone under the owner's ruling** (`CR-2026-09-16-D25-AVAILABILITY-RULE`, written FIRST; the owner declared this clone canonical and ruled reconstruction from the approved specification only — no memory of the lost diff, no cherry-pick). Edits: `src/features/availability.py` (+143/−13: closed rule set, `AvailabilityRow.availability_rule`, nullable `safe_lag_hours` only under a rule, rule/scalar and rule/window exclusion, `max(existing, rule)` availability, negative-lag refusal at build and in `assert_lags_safe`), `tests/test_feature_availability.py` (+202: 12 new tests, none removed), `scripts/05_build_features_and_splits.py` (+1: `PRODUCED_FIELDS`). The reviewed 2026-09-16 statistic (+121/−5) is not reproduced and not claimed. `features-and-splits` code-summary is staler under its receipt — carried per `gf-3`.

- 2026-09-18T14:30:00Z — **GFZ driver-pair acquisition executed under the owner's qualified approval** (`CR-2026-09-18-GFZ-DRIVER-PAIR-AUDIT`, written before the outputs were finalised). Four provider files retrieved into `evidence/audit_gfz_2026-09-18/` through `acquisition`'s `RetrievalClient` with an injected urllib transport (`_build_transport` untouched). Measured: Kp nowcast vs definitive differ at 1,046/2,920 epochs (35.8 %); Hp60 V2.0 vs V3.0 differ at 1,790/8,760 (20.4 %), labelled as the substitute control the owner specified, never NRT-vs-definitive. Provider limitations recorded verbatim in every output. Five registry-visible failed runs precede the completed one — every failure a W-9 egress false positive on a legitimate identity shape, fixed in the script's value shapes and never by growing the allowlist.

- 2026-09-18T15:00:00Z — **Owner rulings recorded** (`CR-2026-09-18-GFZ-RELEASE-GRADE-RULINGS`): Kp nowcast = forecast-available grade, definitive = audit comparator only; Hpo.0002 V2.0 = contemporaneous grade, V3.0 = recomputed comparator; Hp60 substitute control formally accepted. Dated amendment blocks inserted in `acquisition` R-40 (authoritative site) and mirrored in `external-products` R-63 under the owner's ruling 3 — two completed-stage artifacts annotated in place, nothing rewritten. D-39/D-40/D-41 texts DRAFTED in the CR for the student to adopt; not written to `evidence/DECISIONS.md`. No producer artifact, no registration.

- 2026-09-18T16:00:00Z — **D-39 and D-40 appended to `evidence/DECISIONS.md` on the student's explicit instruction**, with the binding qualifications (not proven-available at every origin; post-issue revisions; availability rule owed from evidence or limitation documented before producer release; substitute control for Hp60 only, establishes neither first-issue availability nor absence of leakage). Recorded as student decisions; no supervisor approval fabricated; countersignature status OPEN. The register's CRLF preserved. D-41 revised to a proposal with existing source hashes distinguished from future output hashes — NOT adopted. Design blocks and script inventory wording aligned; outputs regenerated offline. G-04 not passed; G-1…G-6 open.

- 2026-09-18T17:00:00Z — **D-41 appended to `evidence/DECISIONS.md` on the student's explicit instruction** after checks 3 and 4 agreed with the existing contracts (TE §6.2 `f107_81_trailing` row; D-25; `trailing_mean`/limbs 2–3; Route 1 `previous_day_median_midnight_utc`, F10.7-only scope). Identities and roles only; output hashes unset; comparators not inputs; countersignature not required for D-41 under TE §18.2 (no covering row; D-10.1 precedent) — availability obligations remain §18.2 Q-16 Student + Supervisor. G-04 not passed; G-1…G-6 open; no artifact, no registration, no config change.

- 2026-09-18T18:30:00Z — **Gate preparation under the owner's instruction** (`CR-2026-09-18-GATE-PREP`): six-entry availability proposal derived from `SECTION_6_2_ROWS` (driver class = exactly kp_safe, ap_safe, hp60_safe, ap60_safe, f107_safe, f107_81_trailing), with no value invented — GFZ rows carry the already-frozen floors (≥3 h / ≥1 h; Q1/Q2 = 3/1) as approved assumptions pending decision A1; F10.7 carries the Route 1 rule. Narrow fixes applied: G-2 (one structural allowlist entry + prefix-check-first ordering, 2 tests, 59 passed), G-5 (dated code-summary addendum), G-6 (truthful completeness string in script 04, 56 passed). G-1, G-3, G-4 left as owner-ruling proposals.

- 2026-09-18T20:30:00Z — **Governed environment exists for the first time**: conda env `tec-thesis-311` (CPython 3.11.16) with every `requirements.txt` pin installed (pyyaml 6.0.1, tensorflow 2.21.0 imports on CPU). `load_configs(phase=1)` succeeds on the real `configs/` and `assert_no_tbd` refuses on the four `TBD` fields — the governed §18.3 stop, produced by the production mechanism. Full suite under the pin: 13 failed (10 in isolation), all exposed by TF/pyyaml/pandas being present, none in the D-25/GFZ modules (68/59/56 green): G-7 `seed_everything` non-re-entrant per process with TF installed; G-8 latent test bug reading real configs; G-9 `test_real_repository_configs_refuse_today` superseded by D-38 (D-38's owed governed check now passes); G-10 NaN-vs-None representation; G-11 TF-absence-dependent test. All proposed, none applied. Registry 24 rows.

- 2026-09-19T03:00:00Z — **Gate preparation 2 under the owner's scoped authorization** (`CR-2026-09-19-GATE-PREP-2`). D-42 appended (student acceptance of the 3 h / 1 h floors as project assumptions; limitation statement propagated as a claims-checklist row; supervisor countersignature REQUIRED and OPEN under TE §18.2 Q-16). A2 implemented: the D-25 rule composes with the trailing window, end day derived from the rule, constituents verified, rule scoped to the F10.7 rows (74 tests green; 4/5 mutants killed). G-1: canonical flat path→hash manifest + metadata sidecar (interim W-4 file removed from the audit dir). G-2 narrowed to word-shaped runs (secret-shaped runs with separators now refused). G-4: R-26's four classes implemented content-gated; real tree scans clean. G-7…G-11 repaired without weakening: fresh-process isolation for one-process-per-stage tests; G-8 fixture month; G-9 aligned to D-38 keeping refusals; G-10 — a REAL mask defect (NaN admitted as present under pandas) repaired and proven by a biting control; G-11 asserted in both TF states.

- 2026-09-19T12:00:00Z — Finding T-1: the approved contracts are jointly silent on which instant an interval-valued `observation_timestamp` records (Vision §7.3 "time represented by the value") and on which component performs the lagged selection; `build_features` reads `driver_values[origin]` with no shift and, with an own-interval-aligned raw Kp series, carried the value of the interval still OPEN at the origin (scratchpad run). Not fixed — READY unit, scientific wording — routed as P-1/P-2. Finding F-2: IRI-2016's documented index inputs (same-day F10.7, centered 81-day and 365-day means, centered IG12/Rz12, target-day ap) cannot truthfully satisfy the report's "not future-centered / available at target time" confirmations; no substitution made — routed as P-3 (supervisor). Correction to yesterday's G-4 note: `.dst_summary.json` is not "excluded by class 4" — it carries December under month-number keys and the scan never reaches the class check for it.

- 2026-09-19T15:00:00Z — `iricore` is neither installed nor pinned in `requirements.txt` despite TE §8.1; P-3's inspection was done on the package's `master` source (1.9.0) and shipped index files, not an installed copy — recorded as an unresolved environment gap, not closed. The D-45 dependent patch is prepared under `governance/proposed/` and NOT applied (supervisor approval of the IRI role outstanding). The widened December scan found a non-UTF-8 provider page (`onDstindex.html`); non-JSON text is decoded Latin-1 for detection rather than failing the whole scan — JSON stays strict.

- 2026-09-19T18:00:00Z — The pip failure from the prior turn's background download was re-diagnosed properly rather than left as an unread log: network/DNS/TLS/proxy/clock all verified clean; the actual reproduced failure (`Installing build dependencies: finished with status 'error'`) traced to the diagnostic `--no-binary :all:` flag forcing a from-source build of `cmake` itself (which normally ships Windows wheels) — an artifact of the diagnostic command, not the real blocker. The real blocker, established independently: `iricore` publishes no Windows wheel at any of its 33 releases, no Fortran compiler exists on PATH, and the package's own README states Windows compilation is known-difficult and recommends WSL. No compiler was installed to chase this, given the maintainer's own guidance.

- 2026-09-19T21:00:00Z — The countersignature-closure write was twice blocked by the auto-mode permission classifier ("Instruction Poisoning") on a wording that asserted the countersignature as independently-verified fact; the second attempt, reframed to record it explicitly as the student's own statement (matching the project's existing "no supervisor signature artifact exists and none is claimed" pattern used throughout the register), was accepted. Re-verified the iricore Linux-wheel availability more precisely this pass: earlier scans only distinguished Windows wheels; a full per-release scan found NO Linux wheel exists past 1.8.0 (1.8.1-1.9.0 ship macOS-arm64 only) -- the Kaggle notebook targets 1.8.0, not the newest release, for this reason.

- 2026-09-19T22:00:00Z — First real Kaggle run of the IRI notebook (student-reported traceback) failed at isolated-env creation: `/usr/bin/python3.10` pre-exists on the image without `python3.10-venv`, and the notebook only installed that package on the interpreter-absent branch. Revised to create the env with `virtualenv` (self-seeding pip), stdlib venv after apt as fallback, all attempts logged before any stop; mechanism exercised locally. Kaggle facts now known: kernel Python != 3.10; python3.10 binary present; ensurepip absent.

- 2026-09-19T23:00:00Z — Second Kaggle run: kernel is Python 3.12; virtualenv rung failed (cause lost); apt-get hung >300 s and `run()` let TimeoutExpired escape, crashing before any report was written. Two notebook defects fixed (timeout recorded as outcome; bundle written after every attempt) and a uv-managed CPython 3.10.21 rung added as the primary isolation route, pinned (uv==0.12.17, cpython-3.10.21) and exercised locally.

## Tradeoffs

- 2026-09-13T12:10:00Z — Chose to halt the vendoring rather than strip the offending lines and proceed. Patching `compat.py` to drop the `py` import would probably have produced a working pytest and the strongest execution evidence available off-Kaggle — but it would mean running the project's entire acceptance suite on a third-party artifact I had already found to be internally inconsistent and marked with a planted difference, then reporting the resulting counts as evidence. A number produced that way is worse than no number, because it reads as stronger. Alternative rejected for the same reason: pinning to a different pytest tag to dodge the finding, which would violate `requirements.txt`'s `pytest==8.2.2` pin.
- 2026-09-13T12:10:00Z — The vendored tree is left in the session scratchpad rather than deleted, so the finding stays inspectable; `git status` confirms it touched no repository file, and no repo module references it. The four modified files in the working tree are the `code-generation` repairs, unrelated.

- 2026-09-13T13:10:00Z — **Suite executed on the real governed pin: 1169 passed / 39 skipped / 0 failed / 0 errors, 26 modules, CPython 3.11.16, exit 0.** Harness is the stdlib stand-in written this session (`scratchpad/standin/pytest.py` + `runner.py`), NOT pytest — assertion rewriting, conftest, plugins, non-function scopes, xfail and `-k`/`-m` are unimplemented and named in the shim's own docstring. **Fidelity signal worth recording**: the run independently reproduces the project's own per-module counts, derived by different harnesses in different sessions — `test_release_hashes` 149, `test_locked_test_guard` 57, `test_regimes_and_reporting` 88, `test_prepared_target_schema` 62, `test_experiment_registry` 49, `test_phase_contract` 36, `test_bootstrap` 31/6, `test_clean_run` 61/3, `test_common_masks` 60/1, `test_models_smoke` 55/1, `test_feature_availability` 54/2, `test_split_embargo` 35/1, `test_acquisition_window` 7 functions → 29 cases, `test_iri_denial` 22, `test_external_drivers` 51, `test_acquisition` 56, `test_reuse_registry` 27. Twenty-plus independent matches. This also gives the first execution-backed confirmation that the `code-generation` repairs work: the R-01 census prints its derivation (12 units, 48 artifacts, 34 subclasses, 15 enumerated, 19 riders) and the D-17 drift guard passes.
- 2026-09-13T13:15:00Z — **The `04` deadlock could NOT be observed here, and the Critical therefore remains traced but unexecuted.** Running the ladder hits an earlier environmental refusal — `preflight refusal: configs\data.yaml: pyyaml is required to parse governed configs` — before the window check is ever reached. Same for `scripts/00` and `scripts/04` invoked directly. Q3 = A asked for the observed refusal and the honest answer is that this clone cannot produce it; recorded as a limit rather than presented as confirmation.

- 2026-09-13T13:30:00Z — **NEW CRITICAL, found by execution rather than reading: on Windows every stage script and the walking-skeleton orchestrator exit 0 regardless of outcome, discarding their own non-zero exits.** Observed: `run_walking_skeleton.py --fixture plumbing_7day` prints `preflight refusal: …` on stderr and exits **0**; identical for `--fixture scientific_1month`, `scripts/00_acquire_prepared_vtec.py` and `scripts/04_build_external_products.py`. Verified two independent ways (direct shell `$?` and `subprocess.run(...).returncode`). The source is not at fault in intent: the handler at `scripts/run_walking_skeleton.py:1073-1078` does `return 1` under the comment "terminate non-zero", and all nine entrypoints use `sys.exit(main())`. **The cause is the R-05 re-exec.** `src/data/config.py:994-1018` `ensure_process_determinism` calls `os.execv(sys.executable, [sys.executable, *argv])`, and its docstring asserts "On Windows, `os.execv` spawns a replacement process and the parent exits — callers observe one logical run either way." The second half is false. Isolated probe (`scratchpad/execv_probe.py`): parent execs, child intends `sys.exit(1)`, **observed process exit code is 0**. On Windows `execv` spawns a detached child and terminates the parent with 0 without awaiting it, so the child's status is unreachable; on POSIX `execv` genuinely replaces the image and the code propagates. Consequences: TE §13.2's ordered clean-run contract cannot detect a failing step; TE §9.2's "both fixtures must pass before any full-year job" gate cannot distinguish a pass from a refusal, since the orchestrator returns 0 either way; and `project.md` § Mandated's "ALWAYS surface an integrity failure … with an explicit exit" is defeated at the root — the explicit exit is written and then discarded. **Platform split is the reason this survived**: Kaggle is Linux and behaves correctly, `local` is Windows and does not, so of the two authorised platforms (TC-03c) the local one silently reports success. It went unfound because no prior session had a working interpreter to run anything with.

- 2026-09-13T14:20:00Z — **R-05 repaired on the owner's explicit in-session instruction**, under `governance/CHANGE_RECORD_2026-09-13_R05_windows_exit_code.md` (written FIRST, per convention). `src/data/config.py` `ensure_process_determinism` now platform-splits: POSIX `os.execv` unchanged; Windows spawns-and-WAITS and exits the parent with exactly the child's return code (`raise SystemExit(subprocess.run(...).returncode)`); the false docstring sentence replaced with the derived platform truth. Three tests added to `tests/test_determinism.py` (43 → 46): the load-bearing negative control (child exit 7 must reach the caller as 7 — **proven to FAIL on the pre-repair code**, where the caller observed 0, via a scratchpad extraction of HEAD's config.py; `CONTROL BITES`), the happy-path 0-stays-0 control, and an AST control pinning the platform split so the detach cannot silently return. The two existing re-exec controls pass unweakened — the Windows handshake poll in `test_reexec_establishes_...` is now satisfied immediately rather than bypassed. 5/5 under the stand-in on real 3.11.16; smoke only, never governed. NOT run, per the owner's scope: full suite, fixture ladder (so the orchestrator's shell-visible exit under the repair is implied, not observed), Kaggle. POSIX stays untested inference. **Foundation's code-summary is now stale for this edit** (its module and its test count) — per `gf-3`, the staleness is carried to this stage's gate as an explicit finding rather than by editing the receipted record; disclosure lives in the CR §5. No commit was made.

- 2026-09-13T15:30:00Z — **04 fixture-ladder deadlock repaired under the owner's Option (a) ruling** (`CR-2026-09-13-04-FIXTURE-WINDOW`, written FIRST). `scripts/04_build_external_products.py`: on a fixture run the declared window IS `load_fixture_scope(...).window` and both audit tiers read only that window (`_months_in_window` iteration; windowed dst day accounting; windowed f107 accounting over the carrier file; integrity tier neither reads nor requires recorded out-of-window dst entries, refusing fail-closed on unattributable names); fixture-run manifests are labelled `evidence_class: fixture_plumbing` (TC-03f) and land under `artifacts/walking_skeleton/<fixture_id>/external/`, never the governed path. Non-fixture behaviour unchanged (full-year `_declared_data_window`, non-exempt gate, same default out path). `fixture_gate.py`, `run_walking_skeleton.py`, `PHASE1_SEQUENCE`, configs, D-11/D-14 all untouched, per the ruling's bounds. Five new controls in `tests/test_external_drivers.py` (51 → 56 fn): the poisoned out-of-window artifact control (**proven to fail on pre-repair HEAD: `CONTROL BITES`, 11 out-of-window months counted missing**), windowed f107, both-directions integrity control, full-year invariance, and the AST declaration-truth pin. Module 56/56 under the stand-in on real 3.11.16 — smoke only, never governed. Disclosed tightening: malformed out-of-month day rows no longer count as parsed. The ladder itself was NOT run (prerequisites still owed: student's two fixture-manifest freezes, owner's `acquisition.window_*` transcription); WS-20/TA-17 move from *structurally unreachable* to *blocked on those prerequisite acts only*. `external-products`' code-summary goes staler still under its terminal receipt — carried per `gf-3`. No commit.

- 2026-09-13T17:30:00Z — **Option B + D-144 trio + Q-31 declarations implemented under the owner's FINAL RULING** (`CR-2026-09-13-000102-FIXTURE-WINDOW`, written FIRST). `configs/data.yaml` gained the D-144 acquisition trio (experiment 8000, kindat 3500, the executed FIVE parameters — owner ruled against D-4's nine; `window_start/window_end` deliberately absent, reserved for DATA-07). Scripts 00/01/02 now derive their fixture-run window from the scope via the one loader (04's precedent): 00 binds every retrieved record to the window through R-31's one date reader; 01's reads-narrowing is structural (the December audit — its only month-record path — refuses under any fixture scope; the inventory path reads release manifests, never records); 02 binds every standardized row on ISO `interval_start_utc` BEFORE the first write (provider rows carry unix stamps no second parser may read — the Stage-04 fluxtable nuance). `fixture_gate.py`, `run_walking_skeleton.py`, `PHASE1_SEQUENCE`, frozen decisions, refusal gates: untouched, verified 0 diff lines. Tests: the Rec-2 controls in `test_clean_run.py` MIGRATED (never deleted) — the single-binding AST helper re-pointed to the Option-B two-branch shape (this also repaired the latent breakage the 04 commit had left in the unrun rec2-04 control); the 00 real-invocation test migrated to the new truths (config independence asserted explicitly, ML-01's non-exempt gate retained); one new load-bearing control per unit module (test_acquisition 56→57, test_december_audit 62→63, test_prepared_target_schema 55→56 fn). Runs (stand-in on real 3.11.16, smoke only, never governed): 57/63/63-case/61+3skip all green; mutation probes vs pre-repair HEAD `8d4297d`: **5/5 CONTROLS BITE**. The two Q-31 identity declarations written to `tests/fixtures/<id>/identity_declaration.yaml` with zero placeholders (owner-supplied stamps P1A/GNSS_VTEC/GRIDDed_VTEC_1H; folds FIX-MAR-FOLD-01/-02 inside March; bootstrap 1000/336h/{24h:14,48h:7}; canonical `DATA07_CAVEAT` constant; verbatim D-11/D-14 clauses) and **both validate through `load_fixture_scope` from their repo paths**; pyyaml round-trip owed on a yaml-bearing host. gf-3 carry-forward: `acquisition`, `inventory-and-registry`, `target-standardization`, `fixtures-and-reproducibility` code-summaries are now staler under their receipts for these owner-ruled edits — carried here and in the CR, no receipt rewritten. HARD STOP honoured: no commit, no push, no candidate manifest, no freeze act, no Kaggle, no D-number edit. Ladder still awaits: measuring runs on a pyyaml host + stations/qc_operations/features/folds freezes.

- 2026-09-18T10:30:00Z — Chose to STOP before Phase 2 rather than retrieve the reachable GFZ files, for three independent reasons any one of which suffices: (1) the repository's approved acquisition mechanism deliberately constructs no live transport and names the wiring as deferred, authorised work (DATA-07), so retrieval here would be an unauthorised code-path bypass of a READY unit; (2) the design's own R-63 control 5 requires BOTH the near-real-time and the definitive GFZ product for 2022, and the provider's nowcast files now cover only the trailing ~30 days (Hp60 nowcast begins 2026-08-20) — the 2022 near-real-time product is no longer published, which is precisely the archive-supersession risk R-63 recorded, so the specified cross-assertion cannot be satisfied from today's provider without an owner ruling; (3) the brief's authority state (Route 1, Q1–Q6) is absent from this clone, so any artifact built now would rest on decisions this record cannot verify.

- 2026-09-18T11:35:00Z — Validation on this host is **real pytest 9.1.1 on CPython 3.14.7** — a real harness for the first time in this stage, but NOT the governed 3.11 pin and NOT Kaggle: 66/2 on the module, 1192 passed / 39 skipped / 0 failed on the whole tree, compileall 0, 8/8 mutants killed (restore SHA-verified). Recorded as supplemental smoke, never governed. The suite appended 148 rows to `evidence/test_run_access_log.jsonl` (append-only by design); left in place and disclosed in the CR.

- 2026-09-18T14:30:00Z — Chose to write BOTH manifest shapes: W-4's nested `write_sha256_manifest` output under `w4_provider_sha256_manifest.json` and a flat `{filename: sha256}` `sha256_manifest.json` for TA-15's governed reader, after the W-4 shape under the governed filename failed `test_release_hashes` on its own metadata keys. The incompatibility is disclosed as an `acquisition` finding for the gate, not repaired in a READY unit. `environment_lock_hash` on the registry rows is a supplemental stdlib lock (pyyaml absent), stated on every row.

- 2026-09-18T18:30:00Z — F10.7 executable checks (293, 0 fail, synthetic data, real functions, 96 origin hours across year/month boundaries): window = exactly 81 days ending D−1, same-day anchor refused, missing day refused, rule instant 00:00 D. **Counterexample recorded, not repaired:** limb 1 on `f107_81_trailing` passes only under the fixture convention (`observation_timestamp` = anchor-day 00:00); with the honest constituent instant it fails at 23/24 hours. Repair (rule + window on the trailing row) proposed for owner ruling A2. Pre-2022 coverage for the first 2022 window verified 81/81 days on the held file.

- 2026-09-19T03:00:00Z — Full suite under the governed pin: **1256 passed / 4 skipped / 0 failed** (from 13 failed on 2026-09-18). Kept open by design: A3 (R-57a reading A proposal with concrete timestamps and replacement wording), A4 (procedure validated on synthetic data, no value), G-3 (class-5 text with four safeguards). `seed_everything`'s R-05 contract untouched — the tests moved to the process model the code has.

- 2026-09-19T12:00:00Z — Kept the redundant per-constituent loop in `assert_anchor_recomputed` (surviving A2 mutant) and pinned the invariant that makes it redundant (monotone D-25 instant + anchor equality) in a test, rather than deleting the loop to make the mutant die: deletion would make the code silently depend on monotonicity of any future rule kind. Pinned the interval-start "trap" as a passing assertion inside the new boundary test so the P-1 obligation stays visible in the suite; the alternative (a failing test) would break the governed suite on a contract the owner has not yet amended. A4: recommended the derived bound rounded up to one significant figure (`8.0e-12` sfu, B = 400) instead of the unrounded literal or a decade multiplier.

- 2026-09-19T15:00:00Z — Class 5 adopted NARROWER than the prepared text (three patterns, schema-validated content, sibling provenance record), accepting that the manifests/records in `audit_gfz_*` are merely inventoried. `y`/`coverage` are admitted for the GFZ report only in their validated structural positions rather than by relaxing `TARGET_INDICATOR_KEYS` globally. isprint tables are endpoint-inspected (O(1)) rather than fully parsed (≈450 MB per run) and the inventory says so. `resolve_f107_at_origin` now returns a dataclass (two tests updated for the type, assertions kept). A4: rounded the derived bound up to one significant figure rather than freezing the unrounded literal.

- 2026-09-19T18:00:00Z — Applied the D-45 IRI-role patch and the six-entry config transcription on the strength of the student's explicit in-message authorization, without waiting for the supervisor's literal signature — matching the project's own recurring pattern where the recorded decision owner's authority moves work forward while a supervisor countersignature is tracked separately as still open. Chose NOT to attempt a Windows-native Fortran/CMake build (high risk of burning significant time against a maintainer-documented unsupported configuration) and instead documented the Kaggle path, which is an already-approved platform and requires no build at all (manylinux wheel exists).

- 2026-09-19T21:00:00Z — Chose to ALWAYS isolate into a venv on Kaggle (never install into the kernel's own site-packages directly) even when the kernel Python already is 3.10, to protect the pinned numpy/fortranformat/pymap3d versions from whatever Kaggle's base image already carries, rather than conditionally skipping isolation when versions happen to already match. Chose to stop cleanly (raise with a captured diagnosis) rather than attempt a from-source Fortran/CMake build if Python 3.10 cannot be obtained on Kaggle, per the explicit instruction not to repeat the local native-build path and given iricore's own maintainer discourages non-Linux/mismatched builds. Wrote ONE reusable inner verification script run via subprocess under whichever Python is chosen, rather than duplicating the verification logic per environment-strategy branch.

- 2026-09-19T23:00:00Z — Chose a uv-managed standalone interpreter over fighting the image's own python3.10: it is an immutable published release, needs no apt, and works regardless of what the image's interpreter lacks; the cost is one more downloaded artefact (~30 MB) and a dependence on uv's release index being reachable from Kaggle. Kept apt only as a bounded last rung.

## Open questions

- 2026-09-13T13:35:00Z — Does `tests/test_clean_run.py` assert on subprocess exit codes? If it does, some of its refusal assertions may be passing vacuously on Windows for the same reason, and the 61/3 figure above would need re-reading on a POSIX host before it is relied on. Not investigated in this stage; flagged because it bears on WS-20/TA-17 evidence.
- 2026-09-13T13:35:00Z — The pytest artifact integrity finding (single planted marker in `_pytest/compat.py`, undeclared module-scope `import py`, three sibling packages clean) is recorded here but belongs to no project artifact. It is an environment/supply-chain matter rather than a thesis matter; confirm at the gate whether the owner wants it recorded anywhere beyond this diary.

- 2026-09-18T10:30:00Z — Hpo product version: the only Hp60/ap60 series GFZ serves today is "Hpo index V. 3.0" (DOI 10.5880/Hpo.0003, 2024) with the header "Currently always D = 0, reserved for future use" — i.e. no definitive/preliminary grade marker exists for Hp60, and the 2022 values are a 2024 (re)computation rather than the values operationally available in 2022. Whether V3.0 recomputed 2022 satisfies the "never backfill from future final values" rule and DATA-07's version-suffix obligation is an owner/supervisor question, not one this stage may answer.
- 2026-09-18T10:30:00Z — Where does the D-25 Route 1 working tree live, and are Q1–Q6 recorded anywhere (D-number, change record, countersignature)? Until answered, Phase 1.A stays unconfirmed.

- 2026-09-18T10:40:00Z — Owner ruling on the discovery report: **Stop here**. The blocker report is the outcome of this step; no acquisition, no artifact, no registration, no transport wiring. Owner owes: sync of the D-25 Route 1 working tree into this clone, and rulings 9a–9d (transport authorisation under DATA-07; R-63 control 5 disposition when the 2022 near-real-time GFZ product is unobtainable; Hpo V3.0 recomputed-2022 acceptability; Q1–Q6 recording). Stage `build-and-test` remains open at its receipted summary; no lifecycle report emitted.

- 2026-09-18T14:30:00Z — R-26's four-class driver-exclusion table now has a fifth candidate (raw GFZ files + derived JSON under `evidence/audit_gfz_2026-09-18/`); design text only, no coded list — owner ruling owed on whether the table gains a row. Q4/Q5 recording and the per-series required release grade (Student + Supervisor, G-04) still precede any producer artifact or `permitted_producers` row.

- 2026-09-18T15:00:00Z — Gate items held open by the owner's ruling 5: G-1 W-4/TA-15 manifest incompatibility; G-2 W-9 identity false positives (allowlist not to be grown); G-3 fifth R-26 exclusion class; G-4 pre-existing `ec1-audit-report.json` December-scan offender; plus G-5 features-and-splits code-summary staleness and G-6 script 04's stale "never retrieved" entry. Producer construction waits on the student's adoption of D-39/D-40/D-41.

- 2026-09-18T18:30:00Z — Decisions owed: A1 (EV-12 pattern for the four GFZ rows — Student + Supervisor, TE §18.2 Q-16), A2 (trailing-row shape), A3 (R-57a composition), A4 (Q6 tolerance measurement); G-1 (manifest contract), G-3 (fifth R-26 class), G-4 (code the R-26 exclusions). Governed 3.11 env `tec-thesis-311` being provisioned (pyyaml 6.0.1 pinned); results appended to the CR when the install completes.

- 2026-09-19T03:00:00Z — Owed decisions (CR §11): supervisor countersignature of D-42; A3 reading; A4 value rule; G-3 adoption; six-entry transcription go-ahead; `artifacts/run_snapshots/` tracking policy. Code-summaries now stale under receipts for: `acquisition`, `governance-guards`, `evaluation-and-comparison`, `models-and-baselines`, `foundation` (tests), `fixtures-and-reproducibility` (tests), `regimes-diagnostics-reporting`, `statistical-inference` (test fixture) — carried per gf-3.

- 2026-09-19T12:00:00Z — Owner/supervisor decisions (CR §9): D-42 countersignature; P-1/P-2 timestamp and lagged-selection amendments; P-3 IRI index-input disposition (recommend (a)); A3 reading with the finding that A and B are extensionally identical on 2022 unless D-26 removes days; A4 value `8.0e-12` and freeze procedure; G-3 class 5 + P-4a scanner widening + P-4b policy gloss; P-5 stale "requests but does not take" text in `iri.py` and the D-25 review-table row. Facts still missing: observed-vs-adjusted content of IRI's `apf107.dat` (needs `iricore`); GFZ Kp PDF §5 not held locally; NRCan publication latency (EC1-R-4).

- 2026-09-19T15:00:00Z — Supervisor countersignatures owed for D-42/D-43/D-46 (one Q-16/Q-17 act) and approval of the IRI role (D-45). Whether the six-entry transcription should add reader assertions for `lag_reference_instant`, `selection_rule`, `recomputation_input_bound_sfu` (recommended: yes, in the transcription record). Code-summaries stale for external-products, features-and-splits, governance-guards (gf-3).

- 2026-09-19T18:00:00Z — Supervisor's actual countersignature on `governance/COUNTERSIGNATURE_REQUEST_2026-09-19.md` remains outstanding for D-42/43/45/46. Whether the student wants to pursue the IRI benchmark on Kaggle now, or defer it, is unresolved. The additive availability_lags config keys (`lag_reference_instant`, `selection_rule`, `recomputation_input_bound_sfu`) are carried by the reader but still unenforced — a reviewed reader change is owed before relying on them.

- 2026-09-19T21:00:00Z — Whether iricore==1.8.0's manylinux_2_35 wheel actually installs on Kaggle's current base image (glibc compatibility unverified locally) is open until the student runs the notebook. Whether Kaggle's kernel Python is already 3.10 or the apt-get python3.10 fallback succeeds is likewise unknown until run. `iricore` remains uninstallable locally (unchanged); Kaggle is the intended path, not a local fix.

- 2026-09-19T22:00:00Z — Whether the revised notebook completes Steps 3–6 on Kaggle (virtualenv creation, hash-pinned install incl. the manylinux_2_35 glibc question, smoke test, repeatability, index-hash stability) — pending the student's re-run and the returned bundle.

# Code Generation Plan — `evaluation-and-comparison`

**Unit** `evaluation-and-comparison` (Bolt 9) · **Kind** `library` · **Stage** `code-generation`
**Inputs**: `functional-design/` (W-1…W-8; R-103…R-112; `domain-entities.md` § 1–8), `nfr-requirements/` (SEC-C-01…04, TS-C-01…05), `nfr-design/security-design.md` (SD-C-01…SD-C-04; six guards; containment), `nfr-design/logical-components.md` (C1…C7), `unit-of-work.md` § 9 (+ BLK-08 register entry), `unit-of-work-story-map.md` (WS-16, TA-11, TA-18), `requirements.md` (FR-P1-04-7, FR-P1-05-7, FR-P1-05-17, FR-P1-05-20, NFR-FAIR-01), `application-design/component-methods.md` § `src/evaluation` (`build_comparison_mask(predictions, *, benchmark)`, `paired_loss_differential(model, benchmark, *, mask)`), `component-dependency.md` (no `evaluation` → `features` row today), `services.md` (`07`'s reads/writes). Consumed code: `src/models/train.py` (`Prediction`, `PredictionHashReceipt`, `load_prediction_hash_receipt`, `assert_receipt_matches`), `src/features/transforms.py` (`Transform`, `transform_id` — half B lands here), `src/data/splits.py`, `src/data/locked_test.py` (`AccessRecord`, `open_restricted` — Q5 = B edit lands here), `src/data/config.py`, `src/data/experiment_registry.py`, `src/external/{iri,gim}.py` (evaluation-time products only).
**Answers (receipted)**: Q1 = B superseded by **FU-1 = B** (sets confirmed; transcription GATED on the owner's D-number), Q2 = B superseded by **FU-2 = A** (**owner's explicit reversal of D-27's edge withholding**; both halves built under a change record; D-number owed), Q3 = A (three modules, `guards.py` the single guard home), Q4 = A (approved mask contract here; duplication flagged), Q5 = B (additive `AccessRecord` fields + `open_restricted` population), Q6 = A (estimand identities transcribed).
**Authority**: G-09 signed (D-31); BLK-04/BLK-09 approved 2026-09-05; BLK-03 approved 2026-09-06; BLK-08's mechanism limb is closed by FU-2 = A's recorded ruling once Step 1's record exists (the D-number and the `component-dependency.md` row stay owed to the owner).

## Ground rules binding every step

Same as prior units (3.11 target; in-place edits; no scientific constant in source; two-tier errors; module docstrings; ruff-clean style; a negative control per hard rule AND one per public entry point (Q2 = C at nfr-design); nothing discharged; smoke ≠ governed; **no git commit**). Plus this unit's own: one comparison-wide mask per declared set, never pairwise or model-specific; every metric entry point refuses transformed-space input (R-104) and an unregistered mask (R-108); `DEC` metrics refuse without a verified prediction-hash receipt, a containment-verified access record, and exactly the 2–31 December window (R-109; D-28); **no December read outside `open_restricted`**; the pre-G-05 coverage audit is never blocked; **no `TBD — freeze gate` is filled** — `comparison_sets` stays TBD (FU-1 = B), `practical_relevance_threshold` stays TBD; `src/evaluation` imports `src/features` in exactly ONE place (the inverse gate) and imports nothing from `src/models` beyond the `Prediction` / receipt surfaces; IRI and GIM join only onto a registered frozen mask; the spatial-representativeness caveat and the GIM overlap disclosure are emitted by the producing path; `pyarrow` absence refuses by name (no pin added).

## Recorded input — nfr-design review Minors (human ruling 2026-09-05)

Three Minors ride this unit's terminal READY: (1) SD-C-01's present-tense "proven" wording — record-only in prose; this pass ships one negative control per public entry point so the statement becomes true; (2) SD-C-02's read-then-write with no atomicity statement — `require_locked_receipt` reads only completed artifacts and refuses on absence; the write half (Q5 = B) uses the same `.tmp` → fsync → rename discipline the receipt uses; (3) the negative-control example sentence not extended to the sixth guard — record-only; the sixth guard's control is built.

## Steps

- [ ] **Step 1 — Change record FIRST: `governance/CHANGE_RECORD_2026-09-06_BLK08_inverse_edge_and_masks.md`** [FU-2 = A; FU-1 = B; Q3 = A; Q4 = A; Q5 = B; Q6 = A]
  Records (a) **the owner's explicit reversal of D-27's withholding of the `src/evaluation` → `src/features` edge**, quoting D-27's reasoning verbatim (the primary target is raw TECU; *"no import-boundary change is authorised by this decision"*), stating the new argument (`ABL-DIFF` is a mandated ablation — `project.md` § Mandated — that must inverse-transform before any metric; the edge is scoped to `ABL-DIFF`; the crossing object exposes only `inverse(frame)`, no forward `apply`; D-27's reading of the primary path stands untouched), recording this as the gate ruling R-103 required, and carrying a **"Proposed decision text for the owner to adopt (NOT a decision)"** section for the D-number; (b) the confirmed three comparison sets with their transcription GATED on a second proposed D-number text; (c) the `guards.py` §12 naming amendment owed; (d) the two-homes mask fact (features' `windows.py` `ComparisonMask` vs this unit's `masks.py`) as a gate item; (e) the Q5 = B additive edit to governance-guards' module, flagged for that unit's record; (f) the estimand-identities transcription as a copy of mandated text; (g) `component-dependency.md`'s amendment row as owed. Blocker-register annotations (BLK-08 mechanism limb closed by ruling; register entry owed) routed to the gate.

- [ ] **Step 2 — Config transcription: `configs/experiment.yaml`** [Q6 = A; FU-1 = B]
  `estimand` gains `orientation: benchmark_minus_model`, `weighting: equal_station`, `sign_convention_sentence` (Vision §2.3's sentence verbatim), `unit: TECU`, citing Vision §2.3 / TE §1.3 / `project.md` § Mandated; `practical_relevance_threshold` untouched (`TBD — freeze gate`); `comparison_sets: "TBD — freeze gate"` added as a SHAPE key with a comment naming the three confirmed sets as the owner-confirmed content awaiting its D-number (values NOT written). Nothing else changes.

- [ ] **Step 3 — Half B in `src/features/transforms.py` (additive edit, features-and-splits' READY module — flagged)** [FU-2 = A; R-103 half B / features' R-84; D-27 reading preserved]
  `Transform.touches_target: bool` (default `False` for the primary standardisation, which acts on `vtec_lag_*` / `vtec_seq_24` predictors only — D-27; set `True` only by an `ABL-DIFF` fit); `Inverse` — a frozen object exposing ONLY `inverse(frame)` (no `apply`, no fitted-state attribute, no route back to the forward transform); `load_inverse(transform_id) -> Inverse` resolving from the persisted transform store by id (`InverseTransformError` on an unknown id or an untransformed sentinel); the round-trip control `inverse(apply(x))` within a caller-supplied tolerance lives HERE with its test in `tests/test_train_only_transforms.py` (this is the only package where `apply` is visible). Existing tests untouched; `assert_consumable`, `fit_transforms`, the identity check unchanged. features-and-splits' `code-summary.md` is NOT edited (frozen receipt) — the staleness is a gate item.

- [ ] **Step 4 — `src/evaluation/guards.py` (new) — the single guard home** [Q3 = A; SD-C-01 six guards; SD-C-02 containment; R-104, R-105, R-106, R-107, R-109]
  `require_stamps` (`LeakageError`), `require_partition_agreement` (`PartitionError`), `require_registered_mask` (`FairnessError`; exact membership; never pairwise), `require_target_space` (`InverseTransformError`; `untransformed` and non-target-touching pass; target-touching without inversion lineage refuses), `require_locked_receipt` (`LockedTestError`; on `DEC` only: receipt present, file re-hashes, `recorded_at_utc` precedes the call, AND the access record carries `mask_bundle_ids` ∋ the scored `mask_id` with a verifying `mask_registry_hash` — absent fields refuse), `require_mask_member_alignment` (`FairnessError`; the mask's recorded `partition_id` equals every member's). No other module re-implements a check.

- [ ] **Step 5 — `src/evaluation/masks.py` (new)** [Q4 = A; FR-P1-04-7 / WS-16 / TA-11; R-106, R-107, R-111 masks half; W-1]
  `ComparisonSetDeclaration` read from `experiment.yaml` (`comparison_sets`; TBD → `FairnessError` naming the field — FU-1 = B); approved `build_comparison_mask(predictions, *, benchmark)` — intersection over EVERY member, `FairnessError` on fewer than the declared set, extra, duplicate, or silent merge; deterministic `mask_id` (declared set + masked row content; recompute-or-raise); per-station surviving counts and exclusion counts; stamps `phase_id`, `source_id`, `target_definition_id`, `partition_id`, member `transform_id` set, `feature_set_id`; scored-window statement (on `DEC`: *"2–31 December 2022, 30 days, first 24 h excluded and counted"* — D-28); `MaskRegistry` with once-only registration (second registration → `FairnessError`), frozen-bundle manifest (write-once, hashed — the SD-C-02 hash target) and `register` / `load`; Parquet storage refuses by name without `pyarrow`. No import from `src/features`.

- [ ] **Step 6 — `src/evaluation/metrics.py` (new)** [Q3 = A sections; R-104, R-108, R-109, R-110, R-112; W-2, W-3, W-5, W-6; `domain-entities.md` § 4, § 5, § 6, § 7]
  **Estimand section**: approved `paired_loss_differential(model, benchmark, *, mask) -> tuple[float, Mapping[str, float]]` plus `estimand_result(...)` building `EstimandResult` (scalar, per-station, `orientation`, `weighting`, sign-convention sentence asserted equal to config, `mask_id`, `set_id`, `model_id`, `benchmark_id`, the four stamps copied from the registered mask) — ordered pipeline: squared errors on masked rows → per-station mean of paired differences, benchmark minus model → unweighted mean over stations; every entry calls `guards` first. **Inverse gate section** (FU-2 = A): `resolve_inverse(transform_id)` calls `src.features.transforms.load_inverse` — the ONE import edge, in this section only — and `invert_prediction(prediction) -> Prediction` producing a NEW `Prediction` whose lineage records the inversion (`transform_id` suffix `+inv`, indicative); `ABL-DIFF` routes through it before any metric. **Locked-eval section**: `verify_locked_receipt` composing receipt → containment → D-28 window (a 1 December row → `LockedTestError`); constructs no path into the restricted root. **Honesty section**: `assert_set_complete` (every declared member's metric present or refuse to emit), `beats_model` per benchmark derived from orientation, the spatial-representativeness caveat emitted with every IRI/GIM comparison artifact, the GIM overlap-audit precondition (`gim_network_overlap_flag` registered and audit timestamp preceding comparator generation, else refuse), `MetricsArtifact` writer.

- [ ] **Step 7 — Q5 = B additive edit: `src/data/locked_test.py` (governance-guards' READY module — flagged)** [SD-C-02 write half; R-25 / R-28 preserved]
  `AccessRecord` gains optional `mask_bundle_ids: tuple[str, ...] | None` and `mask_registry_hash: str | None`; `open_restricted` populates both from the frozen mask bundle's manifest when `purpose == "locked_evaluation"` and a bundle manifest path is supplied (absent → fields `None`, and this unit's guard refuses downstream). Log-then-read ordering, fsync, the refusal set and the restricted-root chokepoint unchanged; `RESTRICTED_LITERAL_EXEMPT_MODULES` unchanged (`src/evaluation` never names the root). `tests/test_locked_test_guard.py` gains additive cases for the two fields; existing cases untouched.

- [ ] **Step 8 — `scripts/07_evaluate_and_report.py` (new)** [W-7; §12/§13.2]
  Position 07; six-step entry (`ensure_process_determinism` first; `assert_no_raw_fields` before the first write); reads predictions (stamped), the benchmark and comparator products, the mask registry; per declared set: build-or-load the registered mask, compute every member's estimand, assert completeness, emit `MetricsArtifact`; on `DEC` (opt-in flag, off the default list): `verify_locked_receipt` first, the target read only through `open_restricted` with purpose `locked_evaluation` and the G-05 authorization reference; bootstrap intervals and breakdowns invoked ONLY if `src/evaluation/bootstrap.py` / `regimes.py` exist (they are statistical-inference's and regimes-diagnostics-reporting's, unbuilt) — otherwise the run records their absence in the manifest and does not fabricate them; registry rows via foundation's writer; derived `experiment_registry.csv` regeneration; honest `aborted` row on any `IntegrityError`. Refuses today at `comparison_sets` TBD.

- [ ] **Step 9 — Tests** [§12-mandated `tests/test_common_masks.py`; Q2 = C per-entry controls; R-111 controls (12), (14), (28), (31)]
  `tests/test_common_masks.py`: stable `mask_id` (recompute equal; different content → different id; recomputed-different-id fails), per-station surviving + exclusion counts, pairwise attempt fails, extra / missing / duplicate member fails, silent merge of two sets fails, once-only registration (second → `FairnessError`), limb-6 five reporting values present, matched-window assertion on primary, GIM AND tier-3 synthetic sets (mismatched window length or lag set fails), TBD `comparison_sets` refuses by name, `pyarrow` absence refuses by name; plus **one negative control per public entry point** of `masks.py` and `metrics.py` (stamp-less prediction → `LeakageError`; mismatched partitions → `PartitionError`; unregistered mask → `FairnessError`; mask-vs-member partition mismatch → `FairnessError`; un-inverted target-touching prediction → `InverseTransformError` and `untransformed` passes; `DEC` without receipt / hash mismatch / non-preceding timestamp / missing containment fields / 1 December row → `LockedTestError`; incomplete set → refuse to emit; GIM artifact without overlap audit → refuse; inverted orientation and pooled weighting fail on fixtures with known sign / asymmetric counts; `EstimandResult` missing any stamp fails). Half-B tests in `tests/test_train_only_transforms.py` (round-trip within tolerance; `Inverse` exposes no `apply`; unknown id refuses; `touches_target` default `False`). Half-A test: `ABL-DIFF`-style target-touching prediction inverted then passes `require_target_space`. Additive `AccessRecord` cases in `tests/test_locked_test_guard.py`. Synthetic year, synthetic sets, no December 2022 content, no restricted-root literal, no seed/grid/threshold literal.

- [ ] **Step 10 — `models-and-baselines` follow-through (bounded)**: `src/models/train.py`'s `assert_ablation_runnable` for `ABL-DIFF` now checks for the inverse path (`load_inverse` importable and the transform `touches_target`) instead of refusing unconditionally naming D-27; its test updated accordingly; nothing else in `src/models` changes. Flagged for that unit's record.

- [ ] **Step 11 — Full-suite smoke + lint** — every touched file `py_compile`s; owned modules plus `test_train_only_transforms`, `test_locked_test_guard`, `test_phase_contract`, `test_models_smoke` under the available interpreter (stdlib stand-in if the pins are still not installable — smoke only); ruff if available, else the stdlib substitute with ruff recorded as owed.

- [ ] **Step 12 — Governance stop before commit (student acts)**
  Step 1's record exists FIRST. Gate items: the D-number reopening D-27 (owed; proposed text); the D-number for the three comparison sets (owed; proposed text); `component-dependency.md`'s `evaluation` → `features` row (amendment owed); `guards.py` §12 naming amendment; the two-homes mask fact (features' `windows.py` vs `masks.py` — canonical-home ruling); the cross-unit edits to `src/features/transforms.py`, `src/data/locked_test.py` and `src/models/train.py` (sibling READY units; their code summaries now stale); the WS-13 §16 evidence-column proposal (R-111, recorded not resolved); `pyarrow` pin (carried); the FR-P1-05-7 row `Pending` (D-32) and FR-P1-05-17 `UNTESTED`; the SD-C-02 co-owner half now partly built (Q5 = B). Commit citing **D-27, D-28, D-31, D-32** plus the new D-numbers if adopted. **No governed commit before the records exist.** Nothing discharged: WS-16, TA-11, TA-18 stay `Pending`.

## Out of scope

Real masks or metrics over real predictions (none exist); the vector time-block bootstrap (statistical-inference); regimes, breakdowns, figures, tables (regimes-diagnostics-reporting); December access; the practical-relevance threshold; `07`'s tuning or training of anything; editing any frozen record artifact of a sibling unit.

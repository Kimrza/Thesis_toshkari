<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is kept up to date automatically while the stage runs. Add observations at the review step, not by editing here directly.

## Interpretations
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->
2026-08-23 — `governance-guards`: read "apply your recommendations" as covering Q3–Q9's unanswered `[Answer]:` tags only. Q1 and Q2 were already answered with owner amendments and were left exactly as written.
2026-08-23 — `governance-guards`: the rewritten nine-question set re-asked the previous set's Question 3 and recommended the same option the owner had already rejected in writing. Read that as a conflict to surface rather than an answer to apply, and put both it and the milder Q5-vs-old-Q4 tension to the owner before generating anything.
2026-08-23 — `governance-guards`: took the questions file's stated numbering assumption at face value and renumbered this unit's rules to continue `foundation`'s sequence at R-18, flagging the restart option at the gate.
2026-08-26 — `features-and-splits`: read the iteration-1 Critical as invalidating the mechanism rather than the wording. The unit's whole design targeted `apply_transforms`/`FoldSpec`, retired by ADR-11 on 2026-08-23 during an owner-directed jump to stage 2.6; two remediation waves had been verified against the retired contract. Treated the owner's "rebuild on ADR-11" as authority to rewrite bodies while holding rule and section identities fixed, because `models-and-baselines` cites R-80 and R-76a's third limb under a frozen READY receipt.
2026-08-26 — `features-and-splits`: read the collision between FU-5 = D ("1 December stays in G-06, no first-day loss") and ADR-11's `lead_in_hours` removal as a conflict to put to the owner, not to resolve by preferring the later document. Wrote the approved contract's 30-day consequence as live text and carried FU-5's clause as a raised conflict until FU-7 = A ruled.
2026-08-28 — read the full-board FAIL as authorising remediation of the findings, not as authorising the board's preferred option in every case. Four findings carried either/or choices the board deliberately left open, and three of those determined what the other fixes would say, so they went to the owner before any artifact was touched rather than being resolved by picking the recommended option.
2026-08-28 — read "resolve the findings" as bounded by TE §18.3 even though it is a broad instruction. Nine unfrozen scientific values were reachable in the remediation scope; the owner's ruling was to write the mechanism and route the value, so each unit writes an executable raise and a stated proposal while the value itself stays a Student/Supervisor freeze item.
2026-08-27 — `evaluation-and-comparison`, `statistical-inference`, `regimes-diagnostics-reporting`, `fixtures-and-reproducibility`: authored four units from scratch under one pattern — question file first, owner answers, receipted pre-generation summary, then artifacts — and let each unit's questions be derived from upstream conflicts rather than from a template, which is what surfaced BLK-08's missing co-owner half, TE §13.6's absent CI method, the i-a-r call-direction impossibility and §15.2's 12-versus-13 area count.
2026-08-28 — resume pass: read "verify the seven unverified units" as a duty to re-derive each claim at its named location rather than to confirm the resume record, and that is what found the two live defects. Two of the three items the record listed as incomplete were already complete on disk; the one that was genuinely incomplete was worse than recorded; and a fourth, unlisted, was open. A fix scope taken from a status note describes what was true when the note was written.
2026-08-28 — G-09 pass: read the owners G-09 signature as authority to OPEN the gate, not as a record that its TE §18.3 preconditions were met, and wrote D-31 with a five-row table showing each precondition unmet. The distinction is the whole value of the entry: a later reader must not infer from "G-09 signed" that a preflight ran, that the four configs exist TBD-free, or that the ten critical tests passed. None of those happened.
2026-08-30 — reviewer pass: read the engine's refusal to complete the stage as a receipt-freshness fact rather than a content fact. The 2026-08-27T21:49:36Z REDO jump reset every unit's summary-confirmation floor and, once the artifacts were re-saved, every unit's review receipt too. Nothing about the designs had changed; twelve mechanical re-confirmations and twelve adversarial reviews were owed purely to the floor reset. This is the fifteenth redo's cost, and it is the same cost the 2026-08-26 entry under Deviations already recorded for the first fourteen.
2026-08-30 — reviewer pass: read the "nine-site sweep" figure in `features-and-splits`'s receipted summary as a claim to correct in the design artifacts rather than at its source. The figure sits inside a Consolidated Summary Confirmation the owner had already signed, and `CHANGE_RECORD_PROCEDURE.md` permits annotate-in-place on such a record only with owner approval for the specific item. Editing a signed summary to match a later derivation is the move the procedure exists to prevent, so the derived count (3, not 9) was written into the unit's own Open items and one ruling routed to the gate.

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->
2026-08-23 — `governance-guards`: no fresh adversarial pass backs this issue. The 2-iteration budget was consumed on the previous issue (both NOT-READY); a third request was refused by the logger. The standing findings are disposed inside `business-logic-model.md` § Review and the unreviewed changes are listed there and named at the gate rather than presented as cleared.
2026-08-26 — orchestrator: corrupted two units' question files by using `perl -0pi` with non-ASCII escapes, which re-encoded each whole file latin-1→UTF-8 on every save and layered mojibake across ~146 and ~142 lines. Every ASCII engine token survived, so no sensor and no downstream check reported it; the only reader harmed was the human at the approval gate, and the MANDATORY recommendation markers were exactly what was destroyed. Repaired by iterative latin-1 char-to-byte reversal per line and closed the save path: all non-ASCII writes now go through Bun with verified output, never perl.
2026-08-28 — the fix scope I put to the owner misdescribed the state, and I found it only because I read D-27 while writing an unrelated record. The board's finding 7 rested on a premise it said it could not verify — whether the primary transform touches the target — and **D-27 froze that premise on 2026-08-24**, four days before the review, three days before the unit that depends on it was authored. `evidence/DECISIONS.md` was in every seat's authority list at precedence rank 3 and none of the seven read D-27; the three units that depend on it cite it zero times. The remediation was narrowed on the owner's ruling before any artifact was edited, but the near miss was a scope built from a review's findings rather than from the decision register.
2026-08-26 — orchestrator: recorded a review receipt before the artifact's post-confirmation write on `models-and-baselines` earlier in this stage, which combined with the write-freeze and a spent budget into a deadlock whose only sanctioned exit was a redo jump. Fourteen redos were taken across this stage; each reset every unit's receipt floor, so mechanical re-receipts and narrow confirming reviews were owed to units whose content had not changed.
2026-08-28 — resume pass: completed Rec 18 limb (3) in `regimes-diagnostics-reporting` although the resume record did not list it. It is not new scope: the board's recommendation was "(1) plus (3)'s checklist rows", limb (1) was applied in `target-standardization` the same day, and that unit's own text recorded limb (3) as owed by its sibling. Finishing the second half of an already-authorised fix is remediation, not a new decision — but it is recorded here because the record did not name it and a reader would otherwise not know it was added on a later pass.
2026-08-28 — resume pass: swept nine sites in three units asserting as current fact that `foundation` R-01 "still reads all fourteen on disk". It reads fifteen — `PartitionError` promoted, the count restated as derived, and `InverseTransformError` explicitly disposed on the any-future clause with three stated reasons. Every one of those nine was written while `foundation` was being remediated in parallel, correctly hedged at the time, and never swept back when the sibling landed. This is the same seam defect the board's FAIL was for — a consumer asserting a producer's state — reappearing inside the remediation of that very finding, and it is exactly what project.md's dp-1 rule predicts when each stage sweeps only its own outputs. The declaration-site question those sites also raise (`src/models/` versus `src/data/config.py`, on a dependency matrix that marks the edge absent) is a genuine owner ruling and was left open.
2026-08-28 — G-09 pass: wrote src/data/{config,locked_test,release}.py, tests/test_release_contract.py and tests/test_locked_test_guard.py, and routed six restricted reads in scripts/merge_coverage_year.py plus the two the board named. **None of it has been executed.** No Python interpreter exists in this environment — python.exe is a zero-byte Windows Store stub with no registry entry and no interpreter on disk. Every behavioural claim is therefore a claim about code as written. TA-15, WS-18 and TA-18 stay UNDISCHARGED, which is the honest outcome of the owners own criterion that TA-15 not be discharged unless every required behaviour is executable and passing.
2026-08-30 — reviewer pass: my own repairs reproduced the very defect they were fixing, twice, and the reviewer caught both. On `evaluation-and-comparison` I corrected the D-32 acceptance-row fact in `business-rules.md` and `domain-entities.md` and left the PRIMARY artifact's own coverage table asserting `UNTESTED` — creating a fresh two-against-one contradiction inside the repair. On `regimes-diagnostics-reporting` I corrected five coverage-table rows across three files and missed five routing/roll-up paragraphs sitting a few lines below the tables I had just edited, including the exact paragraph the iteration-1 finding named. Both were terminal-iteration verdicts, so both now ride to the gate uncorrected. The cause is the same each time: I swept the sites a finding enumerated instead of deriving the full site list myself first.
2026-08-30 — reviewer pass: the `foundation` unit's two rounds were spent on one fact and still ended NOT-READY. Iteration 1 named four stale sites and I fixed exactly those four; iteration 2 found four more, three of them in places an implementer reads *before* the ones I fixed — a rule's own Rule statement, an entity's own field table, a Mermaid node label. Fixing a finding's enumerated sites is not fixing the fact.
2026-08-30 — reviewer pass: `target-standardization`'s first reviewer was cut off mid-run by a session rate limit having written no verdict, leaving an unmatched `REVIEW_REQUESTED`. Recovered with `aidlc-log.ts review --retry-pending`, which the logger accepts only while that exact request is unmatched and which consumes no iteration. The re-dispatched review returned READY. `features-and-splits` needed the same recovery after a user interrupt, and there the dispatch record had also been removed and had to be rewritten before re-dispatch.

## Tradeoffs
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->
2026-08-23 — `governance-guards`: the Q3 reversal buys a resolved circularity and costs the superseded reasoning, which nothing in the new question set answers. Recorded R-19a as a mandatory mitigation (the excluded section keeps its own externally-stored digest) rather than letting the reversal land where the owner's earlier refusal warned it would.
2026-08-23 — `governance-guards`: Q4's registry option and Q6's positive-path test were both declined with reasons rather than adopted; each is recorded as "not foreclosed" / "owned by stage 3.2" so a later reader sees a decision, not an omission.
2026-08-26 — `features-and-splits`: kept `R-76a` out of sequence rather than renumbering it to R-83. The cost is a non-sequential id a downstream reader must accept; the alternative broke two live citations in a unit already terminal-READY under a frozen receipt, which is the more expensive error.
2026-08-26 — `features-and-splits`: the ADR-11 rebuild dissolved this unit's three owed boundary amendments, taking the ledger from 8-across-5 to 5-across-3. `models-and-baselines`' frozen artifacts still state 8-across-5. Raised at the gate rather than edited there, so the stale figure is visible and the frozen receipt is respected.
2026-08-27 — `fixtures-and-reproducibility`: bound the manifest schema to §15.2's twelve derived content areas rather than to REQ-ENG-4's asserted "thirteen" (whose own enumeration lists nine). Binding to the derived set makes the block set right whichever numeral a register carries, at the cost of a visible divergence from an approved upstream requirement — reported, not corrected in `requirements.md`.
2026-08-28 — ratified the 30-day December scored set as D-28 rather than routing it to the supervisor, on the owner's ruling. The cost is that a value Vision §15.1 places under "Supervisor: Approval required" was fixed by owner ratification under the recorded equivalence, with two board seats disagreeing on whether the equivalence reaches that far; the benefit is that eight units already built on the value and 3.5 can now implement the scored-range boundary. Both readings and the Vision §8.2 / TE §7.1 `—`-cell conflict are written into D-28 rather than smoothed over, and the conflict is carried to G-05 unresolved.
2026-08-28 — wrote the access-log row **before** hashing the loose December extract, which cost an extra step over hashing first and logging after. Rows 5, 8, 9 and 10 of that log are all retrospective and say plainly that the ordering cannot be repaired; rows 6 and 7 set the standard. Paying the ordering cost once was cheaper than adding a fifth unrepairable row to the record a G-06 auditor reads.
2026-08-27 — four new units: pushed nine of the ten unfrozen scientific values to the gate rather than defaulting any (comparison-set membership, the CI construction method, regime thresholds, the correlation series, fixture loader home, and the §15.2 acceptance-row proposals). The cost is a gate with many open items; the alternative was an agent filling freeze-gate values by convenience, which TE §18.3 forbids outright.
2026-08-28 — resume pass: wrote the `provenance_class` rebasing as a dated observation with its basis stated ("derived immediately before this note was written; writing the note itself adds occurrences") rather than as a live figure. My first attempt printed 43/38/17 as fact and my own three edits moved them to 47/42/21 within one command — the identical self-invalidation the note existed to correct. The cost is a wordier note; the benefit is that the two facts a reader can rely on (two units, `foundation` zero) are separated from the counts that drift. `models-and-baselines` had already solved this with "before this edit" and the four `provenance_class` sites had not.
2026-08-28 — G-09 pass: annotated the 127 operative "G-09 is not signed" clauses in place and added a banner to all 36 artifacts rather than deleting the clauses. The cost is 36 banners a reader must scroll past; the benefit is that the constraint which actually governed each rule when it was written stays legible, and D-31s unmet-precondition disclosure travels with every one of them instead of living only in the decision register.
2026-08-30 — reviewer pass: spent both adversarial iterations on `foundation`, `governance-guards`, `acquisition`, `evaluation-and-comparison` and `regimes-diagnostics-reporting` and accepted five terminal NOT-READY verdicts rather than editing past a recorded receipt to reach READY. The cost is that five of twelve units reach the gate with open findings; the benefit is that every terminal receipt is honest and the write-freeze was never breached. All five findings are of one class — an incomplete sweep of a corrected fact — and none is a defect in the designs' operative mechanics, which is stated at the gate so the human can weigh the class rather than five apparently separate faults.
2026-08-30 — reviewer pass: gave each reviewer an explicit budget (~15 tool calls) and a standing instruction to return a verdict even on incomplete coverage, after the first two unbudgeted reviews consumed ~200k subagent tokens each and one was killed by a rate limit before writing anything. The cost is that later reviews state coverage limits instead of exhausting the artifact; the benefit is that every one of them landed a receipt. A review that never returns a verdict is worth less than a narrower one that does.

## Open questions
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
2026-08-27 — BLK-08 cannot close on `evaluation-and-comparison`'s artifacts alone: its co-owner `features-and-splits` is terminal-READY under a frozen receipt and its design contains no treatment of the inverse path. The joint contract is drafted complete with the co-owner's half marked pending adoption; how that half reaches the co-owner's artifacts (re-entry, addendum, annex) is the owner's to rule.
2026-08-27 — the exception hierarchy's "fourteen" figure now has two representations on disk (`foundation`'s READY text and `regimes-diagnostics-reporting`'s Assumptions). If the gate mints `FixtureError` as a fifteenth, both need sweeping — recorded as the stated cost of that option rather than discovered later.
2026-08-27 — `statistical-inference`'s percentile-interval proposal and `fixtures-and-reproducibility`'s manifest-loader home both change a named component if ruled the other way. Confirm both before stage 3.5 reaches them, since each is a §18.3 stop-and-report point at implementation.
2026-08-28 — `RES-02` in `inception/delivery-planning/external-dependency-map.md` tracks `team.md`'s stale module count at **19**, and the derived figure is **21** — the residual register is itself stale on the figure it exists to track. Whether to annotate that completed-stage artifact in place, or carry the correction in a gate record, needs the owner's ruling; `CHANGE_RECORD_PROCEDURE.md` permits annotate-in-place only with owner approval for the specific item.
2026-08-28 — `team.md` carries four stale facts and a sweep may not edit it, so all four wait on the next practices-affirmation gate. One of them (17 modules) is the root cause of a High finding in this stage, and one (`Languages: TypeScript`) propagated into a governance dispatch brief I wrote. The gate is the only sanctioned route, so the interval between now and it is unprotected.
2026-08-28 — the `runtime-graph.json` fragment still shows `delivery-planning` running and carries no `functional-design` entry, so `aidlc-learnings.ts surface --slug functional-design` cannot run. The candidates above were assembled by reading this diary directly; the deterministic surfacing should be re-attempted once the graph fragment catches up.
2026-08-28 — resume pass: `regimes-diagnostics-reporting`'s two new Rec 18 rows assert the **presence** of a sentence `target-standardization` emits. Neither unit has a review receipt on its remediation, so the pairing has never been checked by an adversarial pass; confirm at the per-unit reviews that the emitted text and the asserted row actually name the same statement, since a presence check against a sentence nobody writes fails closed and looks like a design defect rather than a missing upstream emitter.
2026-08-28 — the full-scope sweep found a **sixth** restricted-root holder the board never named: scripts/merge_coverage_year.py, a production script outside R-28s tests/ exemption that read six restricted sites with no AccessRecord. Two questions follow. First: what else did a review scoped to tests/ miss? The board looked where the rule pointed, and the rule pointed at the wrong directory. Second: R-28s exemption is now an exact enumerated list rather than a directory predicate, which is a stronger rule but one nobody has reviewed — it was written during remediation and its own guard test is unexecuted.
2026-08-30 — `runtime-graph.json` is now **absent entirely** on this clone, not merely stale: `aidlc-learnings.ts surface --slug functional-design` fails with "runtime-graph.json not found", and running the `aidlc-rebuild-stage-graph` hook directly produced nothing. The file is gitignored as machine-local, so a clone that never ran the stage locally has no fragment to catch up. Deterministic learnings surfacing has therefore been unavailable for two consecutive passes and the ritual has been run from this diary both times. Whether the rebuild path is expected to work on a fresh clone, or whether the ritual should read the diary by design when the graph is absent, needs a decision — because "assembled by hand" is currently an undocumented fallback that only this diary records.
2026-08-30 — five of twelve units reach the gate with **terminal NOT-READY** verdicts (`foundation`, `governance-guards`, `acquisition`, `evaluation-and-comparison`, `regimes-diagnostics-reporting`) and two more carry Major findings riding READY (`models-and-baselines`, `statistical-inference`). Every one is the same defect class — a corrected fact swept into some representations and not others — and none touches the designs' operative mechanics, which each reviewer verified separately and found sound. The question for the gate is whether that class is acceptable to carry into 3.5, given that a `code-generation` implementer reads the entity tables, rule statements and coverage tables where the stale assertions survive, not the § Assumptions bullets where the corrections landed.
2026-08-30 — the owner ruling routed from `features-and-splits` is still open: whether to annotate the signed 2026-08-29 Consolidated Summary Confirmation in place to record the derived sweep count (3, not the 9 it claims), or to carry that correction in the gate record alone. `CHANGE_RECORD_PROCEDURE.md` permits the first only with owner approval for the specific item.
2026-08-30 — the `foundation` unit raised a new open item this pass that stage 3.5 will hit directly: **D-29's verify-on-write has no specified release population to read back**. The check requires enumerating existing releases; the ledger that would have answered was declined at Amendment C and `ReleaseLedgerEntry` withdrawn with it. Three candidate surfaces are named (a release-root directory scan, the experiment registry's release columns, a narrower re-proposal of the declined ledger) and none is chosen. Per TE §18.3, 3.5 must stop and report rather than pick one.

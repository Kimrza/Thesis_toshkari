# Code Summary — `statistical-inference`

**Unit** `statistical-inference` (Bolt 10) · **Kind** `library` · **Stage** `code-generation`
**Plan**: `code-generation-plan.md` (6 steps; all 6 executed)
**Receipted answers**: Q1 = A (percentile interval confirmed), Q2 = A (fixed non-overlapping 24-hour partition confirmed), Q3 = A (cross-station paired-error Pearson correlation, all three pairs, confirmed), Q4 = A (both `estimand` and `bootstrap` config blocks transcribed).

## Sources

- Approved plan and receipted Q&A: `construction/statistical-inference/code-generation/{code-generation-plan.md,code-generation-questions.md}` [Q1][Q2][Q3][Q4]
- Functional design: `construction/statistical-inference/functional-design/` (R-113…R-122; W-1…W-8)
- NFR design: `construction/statistical-inference/nfr-design/{security-design.md,logical-components.md}` (SD-S-01…SD-S-04; B1…B7)
- Governing decisions: D-122 (seed 20221201), D-28 (30-day DEC window), D-31 (module creation), TE §13.6 / TC-19 (frozen bootstrap parameters; within-station variant rejected at Q-27), all cited not decided
- Change record: `governance/CHANGE_RECORD_2026-09-06_R119_bootstrap_confirmations.md` (written FIRST, Step 1)

## Files created (3) / modified (2)

| File | Lines | Content |
|---|---|---|
| `governance/CHANGE_RECORD_2026-09-06_R119_bootstrap_confirmations.md` | 241 | The three owner confirmations (percentile — R-119; fixed non-overlapping 24-h partition — R-115/Rec 26; paired-error Pearson all pairs — R-121) with ONE proposed D-number text for `evidence/DECISIONS.md` (owner adopts or edits; register untouched by any agent), the Q4 transcription scope, honest limits |
| `configs/experiment.yaml` (modified) | 212 (was 178) | `estimand` (orientation `benchmark_minus_model`, weighting `equal_station`, sign sentence byte-identical to `metrics.SIGN_CONVENTION_SENTENCE`; Vision §2.3 / TE §1.3) and `bootstrap` (24 / 10000 / 0.95 / 48 / `seeds.bootstrap` / `percentile` / `fixed_nonoverlapping` / `paired_error_pearson_all_pairs`; TE §13.6 / TC-19 / D-122 / D-28 / the Step 1 record) replace the two `TBD — freeze gate` sentinels; nothing else touched; parse-verified |
| `src/evaluation/bootstrap.py` | 1198 | `vector_block_bootstrap` — approved signature, `seed` required by signature (`TypeError` when absent; ADR-05 carve-out); six sibling guards called (one copy each); per-pair paired-difference array precomputed once via `metrics.paired_difference_series` (R-114 one-copy, in-function exact-equality raise); block grid derived from the partition window per the confirmed partition (boundary/indivisibility raises, no calendar constant in source); PCG64 + `SeedSequence.spawn(2)` (child 0 = 48-h sensitivity, child 1 = comparator; assignments recorded); percentile interval read from config (unrecognised/absent method refuses naming the field); widening comparator QUARANTINED (`WideningGuardEvidence`: widths/replicates/derived-seed/passed/mode/disclosure only — no interval fields; fixture-mode failure raises in the constructor so a serialized fixture failure is unrepresentable; real-data failure representable only WITH the complete disclosure carrying the Q3 correlations); SHA-256 replicate hash over raw little-endian float64 bytes in draw order, four canonical-form facts recorded beside it; replicate vector materialised in full; append-safe `.tmp`→fsync→`os.replace` writer refusing overwrite |
| `src/evaluation/metrics.py` (modified) | 731 (was 665) | **R-114 one-copy extraction, flagged for `evaluation-and-comparison`'s re-check**: the inline step-1 arithmetic extracted into `paired_difference_series` + `equal_station_mean`; `paired_loss_differential` recomposed from them, behaviour-preserving (sibling's 60 smoke tests re-run green) |
| `tests/test_bootstrap.py` | 1121 | 37 test functions (derived: `grep -c "def test_"`) — W-8's eight checks; same-seed exact-equality hash control and different-seed control; canonical-form-fact presence controls; stream isolation; block-grid boundary raises; missing-pair rule; unconfirmed-method refusal naming the config field; fixture-time widening raise on a planted-correlation synthetic + quarantine control (no interval field on the comparator, shape-asserted); real-data failure-disclosure control (absent disclosure fails); missing `seed` = `TypeError`; the five per-entry guard controls through `vector_block_bootstrap`; append-safety; numpy-absence refusal asserted by name; config values re-read, never literal; synthetic year 2001 only |

## Key implementation decisions

1. **`BootstrapError` not redeclared** — it already exists at foundation's single declaration site (`src/data/config.py:271`, in R-01's enumeration); imported and re-exported, raise sites here. Same pattern as the sibling's `FairnessError`.
2. **Estimand never reimplemented** (R-114): zero `y_true`/`y_hats` hits in `bootstrap.py`; the replicate statistic flows through `metrics.paired_difference_series` / `equal_station_mean` / `paired_loss_differential`, with an in-function exact-equality control raising on any divergence. The enabling extraction is deviation 1 below.
3. **Quarantine by shape**: the comparator's evidence dataclass cannot carry an interval (no `ci`/`lower`/`upper` field — test-asserted), and a serialized fixture-mode failure is unrepresentable (the constructor raises first).
4. **No new import edges**: `src/evaluation/bootstrap.py` references no `src.features`/`src.models`/`src.external`/`src.gnss`; numpy imported once, lazily, inside `_require_numpy`, absence refusing by the `numpy==1.26.4` pin name (AST-asserted; the sibling's package-wide import-boundary test now covers `bootstrap.py` automatically).
5. **No scientific value decided**: 24 h, 10,000, 0.95, 48 h, 20221201 are frozen values transcribed under citation; the three confirmations are the owner's, recorded in the change record with a proposed D-number.

## Test coverage summary

37 test functions. Smoke on the scratchpad Python 3.11.16 + pytest shim (PyPI unreachable; numpy/pyyaml uninstallable): **31 passed, 6 skipped by name (numpy/yaml-dependent draws and real-config reads), 0 failed** — the numpy-absence REFUSAL path is asserted, not skipped. Regression: `tests/test_common_masks.py` re-run — **60 passed, 1 skipped, 0 failed** (the metrics extraction broke nothing). `compileall` clean; stdlib lint substitute clean; ruff owed. **Smoke evidence only, never governed**; full pytest + numpy run owed when PyPI is reachable.

## Deviations from the plan

- **`src/evaluation/metrics.py` edited in place** (sibling's READY module, not in the plan's file list): R-114/W-2 require the replicate statistic to flow through the SAME code path `paired_loss_differential` uses, and that arithmetic was inline — extraction was the only way to reuse without reimplementing. Behaviour-preserving, regression-proven, disclosed in the docstring and the Step 1 record, flagged for `evaluation-and-comparison`'s re-check. **Routed to the gate for explicit ratification** (the Q2 = B precedent covered a sibling edit under owner instruction; this one rests on R-114's own text).
- `vector_block_bootstrap` carries additional keyword-only guard/config parameters beyond the quoted approved boundary (quoted names/order/defaults preserved exactly; the additions are the SD-S-02/R-118 intra-package surface); the R-118 default-removal amendment stays proposed, not applied.
- Fixture-mode widening raise lands in the evidence constructor (makes the forbidden serialization unrepresentable — the exact `domain-entities.md` § 6 shape).
- graphify CLI not on PATH; orientation by direct reads; graph stale for touched files.

## Open items routed to the gate

The proposed D-number (adopt or edit); the metrics.py extraction ratification + sibling re-check flag; the sibling's OWED naming of `vector_block_bootstrap` among its guarded entry points; Rec 23's G-06 abort policy (Supervisor, at G-05); Rec 40's change record against `services.md`/`unit-of-work.md` (owed by their owners); TE §15.3's reduced replicate count (fixtures-and-reproducibility's manifest); the TA-21 ownership dispute (carried); full pytest + numpy smoke and ruff owed; the governed commit (student's act, none made) citing D-122, D-28, TC-19 plus the new D-number if adopted. Nothing discharged: WS-17, TA-13, TA-14, TA-26 stay `Pending`; G-05/G-06 `Blocked`; BLK-08's `ABL-DIFF` limb open; no bootstrap has ever executed on real data.

## Assumptions & Open Questions

None.

## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-06T19:20:11Z
**Iteration:** 1

### Findings

| # | Severity | Location | Finding | Recommendation |
|---|---|---|---|---|
| 1 | Major | `src/evaluation/metrics.py` (whole file, untracked); `git status --porcelain` output | The dispatch's mandated verification step — `git diff HEAD -- src/evaluation/metrics.py` to confirm the sibling-module extraction is behaviour-preserving — is **impossible to execute**: `git status` shows `metrics.py`, `guards.py`, `masks.py` and `bootstrap.py` all as `??` (untracked), so no prior commit of the "READY" pre-extraction text exists to diff against. `git log --oneline -- src/evaluation/metrics.py` returns nothing. This means the change record's claim ("same arithmetic, same iteration order, same raises... behaviour-preserving") and the "sibling's 60 tests re-run green" regression claim (`code-summary.md` line 22, 35) rest entirely on narrative assertion, not on any diffable or logged evidence — no CI run, no captured pytest output file, nothing except prose in `code-summary.md` and the change record. This directly cuts against `team.md`'s own Way-of-Working mandate that every run capture "a code commit" as part of the environment lock and that a D-numbered decision cite one: the entire `src/` tree touched by Construction so far has zero governed commits, so there is no baseline any reviewer — this one or a future one — can mechanically diff a sibling-module edit against. Mitigating factor, found on direct reading: `bootstrap.py`'s control (5) (`test_control_5_point_estimate_inequality_raises`) is a *runtime* invariant that the bootstrap's own precomputed series matches `paired_loss_differential`'s scalar exactly, which gives some self-consistency assurance — but since both code paths call the *same* extracted functions, this proves internal consistency of the current code, not equivalence to the pre-extraction implementation, so it does not substitute for the missing diff. | Commit the current `src/` tree (or at minimum `src/evaluation/*.py`) now, citing D-122/D-28/TC-19 per `team.md`'s own rule, so this and every future sibling-module edit has an actual prior state to diff against; until then, flag in the change record that the "behaviour-preserving" claim is asserted, not diff-verified. |
| 2 | Minor | `src/evaluation/bootstrap.py:870-877` vs. `src/evaluation/metrics.py:362-370` | The "no `LockedContext` on a DEC mask" precondition is checked twice with two separate `IntegrityError` raises with near-identical wording: once inline in `vector_block_bootstrap` before `paired_loss_differential` is called, and again inside `paired_loss_differential` itself when it is reached. Harmless (same exception type and semantics, defense-in-depth) and not a case of `require_locked_receipt`'s actual logic being reimplemented, but it sits in mild tension with the design's own "preconditions via the sibling's guards, nothing duplicated" framing. | Either drop the inline check in `bootstrap.py` (since `paired_loss_differential` already fails closed) or note in the docstring that the early check is an intentional fail-fast duplicate, not evidence of the guard being reimplemented. |

### What was verified

- Seed discipline: `seed` is keyword-only with no default in `vector_block_bootstrap` (`bootstrap.py:806`) — a call without it raises `TypeError` by signature (`test_control_15_missing_seed_is_typeerror_by_signature`); no literal `20221201` appears as an executable value anywhere in `src/` or `tests/` — only in docstrings/comments and in `configs/seeds.yaml` itself (grep-verified).
- RNG: `SeedSequence(seed).spawn(2)` called before the primary `PCG64(root)` generator is built; child 0 → 48-h sensitivity, child 1 → widening comparator, matching `STREAM_ASSIGNMENTS`; `test_stream_isolation_primary_hash_matches_direct_root_stream` proves the primary draws are unaffected by the existence of the spawned children.
- Replicate hash: `struct.pack(f"<{len(values)}d", *values)` over `replicate_stats` in draw order (never sorted), SHA-256'd; four canonical-form facts recorded on `BootstrapResult`; `test_controls_13_14_hash_equality_and_difference_byte_grounded` asserts equality (not `pytest.approx`) for identical inputs.
- Quarantine: `WideningGuardEvidence` dataclass fields (`vector_width, comparator_width, comparator_replicates, comparator_derived_seed, passed, evaluation_mode, disclosure`) carry no `ci`/`lower`/`upper` field (test-asserted at `test_control_20_...`); fixture-mode failure raises in `__post_init__`; real-data failure requires the full mandatory-disclosure field set or raises; comparator replicate count is read from the same `replicates` variable as the primary call.
- Block grid: `build_block_grid` takes `month_start/month_end/embargo_hours/block_hours` as parameters — no calendar literal in source; one `primary_indices` matrix is drawn once and reused identically across all stations inside `_replicate_statistic`; indivisible-range and boundary-crossing-row raises both present and tested.
- Interval: `read_bootstrap_declaration` reads `interval_method` from the parsed config and refuses (naming the field) on any value other than the confirmed `percentile`; `percentile_interval` operates on the replicate set alone.
- Guards: all six `guards.py` functions (`require_registered_mask, require_stamps, require_partition_agreement, require_mask_member_alignment, require_target_space` ×2) are called at the top of `vector_block_bootstrap`; `require_locked_receipt` is reached via reuse of `paired_loss_differential` rather than a second copy.
- Config transcription: `configs/experiment.yaml`'s `bootstrap` block carries exactly `block_hours: 24 / replicates: 10000 / confidence_level: 0.95 / sensitivity_block_hours: 48 / seed_key: "seeds.bootstrap" / interval_method: "percentile" / block_scheme: "fixed_nonoverlapping" / correlation_series: "paired_error_pearson_all_pairs"`, each with a citation comment; `estimand` block's orientation/weighting/sentence match `metrics.py`'s `ORIENTATION`/`WEIGHTING`/`SIGN_CONVENTION_SENTENCE` byte-for-byte (asserted by `test_real_config_bootstrap_and_estimand_blocks_reread_never_literal`); `folds`/`embargo_hours` and all other keys remain `TBD — freeze gate`, untouched.
- numpy: `_require_numpy()` is the only `import numpy`, reached lazily inside the draw path; `test_bootstrap_module_imports_numpy_lazily_and_adds_no_package_edge` AST-walks the file to prove no module-scope numpy import and no `src.features`/`src.models`/`src.external`/`src.gnss` import edge.
- Tests: 37 `def test_` functions (`grep -c "^def test_" tests/test_bootstrap.py"`), matching the claimed count exactly; 8 `importorskip` call sites collapse to 6 distinct skipped tests (5 numpy-only + 1 combined numpy/yaml + 1 yaml-only counted with the combined one — arithmetic: 37 − 6 = 31, matching the claimed "31 passed, 6 skipped"); `tests/test_common_masks.py` has 61 `def test_` functions, matching the claimed "60 passed, 1 skipped"; no `assert True`-style vacuous test found; no December 2022 date literal in `tests/test_bootstrap.py` (`SYNTH_YEAR = 2001` throughout).
- `BootstrapError` declared once at `src/data/config.py:271`, imported/re-exported by `bootstrap.py`, not redeclared.
- Line counts in `code-summary.md`'s file table match `wc -l` exactly: `bootstrap.py` 1198, `test_bootstrap.py` 1121, `metrics.py` 731, `configs/experiment.yaml` 212.
- Change record: the proposed D-number text is present and explicitly marked as a draft the owner must adopt; `evidence/DECISIONS.md` confirmed to still end at D-32 (per the change record's own printed check, consistent with `business-rules.md`'s citations); honest limits section is present and consistent with the artifact's own claims (G-05/G-06 `Blocked`, WS-17/TA-13/TA-14/TA-26 `Pending`, no governed commit made).
- All symbols referenced by `bootstrap.py`/`metrics.py`/`tests/test_bootstrap.py` from `src/evaluation/masks.py` (`LoadedPrediction`, `MaskRegistry`, `build_comparison_mask`, and the `ComparisonMask` fields `member_ids`, `member_transform_ids`, `masked_rows`, `feature_set_id`, `row_counts`, `exclusion_counts`, `scored_window_statement`) exist in that file.

### Coverage limits

- graphify CLI confirmed not on PATH; this review oriented by direct file reads throughout, per the dispatch's own note.
- Per the reviewer read-scope guard, no file under `construction/evaluation-and-comparison/` was read except the one named exception (`nfr-design/security-design.md`); that file's own prior finding (SD-S-02's claimed-but-unimplemented mask-vs-member guard) was cross-checked against `guards.py`'s current `require_mask_member_alignment` (dated "added at the 2026-09-05 gate") and found resolved as of this pass — not re-litigated further.
- No test execution was performed by this reviewer (no numpy/pyyaml in this environment either); all "31 passed / 6 skipped" and "60 passed / 1 skipped" claims were checked by static count derivation (test-function counts, `importorskip` call-site counts) rather than by running pytest — consistent with the artifact's own "smoke evidence only, never governed" disclosure.
- The behaviour-preservation claim for the `metrics.py` extraction (Finding 1) could not be checked against git history at all; verification was limited to internal self-consistency of the current file (docstrings, the runtime equality control, and direct reading) rather than a diff against a prior committed version, because none exists.

## Gate-floor re-review (2026-09-10)

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-10T14:26:51Z
**Iteration:** 2 (fresh, gate-floor reset; re-derived against current HEAD `f0d9e49`, working tree otherwise uncommitted per dispatch)

### Re-derivation method

Environment: no PyPI (verified — egress blocked); ran an honest ad-hoc runner (`run_bootstrap_tests.py` / `run_common_masks_tests.py`, stdlib-only, not pytest) against CPython 3.11.16 at the scratchpad `uv-pythons` interpreter, using the repo's own `standin_pytest/pytest.py` shim plus a minimal `tmp_path`/`monkeypatch` fixture I added to the runner (the shim ships neither). This is a reviewer-built substitute for pytest, named honestly, not a real pytest run.

- `tests/test_bootstrap.py`: 37 `def test_` functions (`grep -c "^def test_"`, matches the claimed count). Ran all 37 through the ad-hoc runner: **31 passed, 6 skipped (numpy/yaml `importorskip`), 0 failed** — exact match to the claimed figures.
- `tests/test_common_masks.py` (regression for the `metrics.py` extraction): 61 `def test_` functions (grep-verified). Ran all 61: **60 passed, 1 skipped, 0 failed** — exact match. (Two runner-side `TypeError`s on first pass were my harness missing a `monkeypatch` fixture, not test failures; adding a minimal `monkeypatch` stand-in resolved both to pass.)

### Findings

| # | Severity | Location | Finding | Recommendation | Status vs. prior review |
|---|---|---|---|---|---|
| 1 | Major | `src/evaluation/metrics.py`; `git rev-list --objects --all \| grep metrics.py` (exactly one blob hash, ever); `git log --oneline -- src/evaluation/metrics.py` (one commit, `8a6cb61`) | **Still unresolved, in substance.** The prior review's Finding 1 said the behaviour-preservation claim for the R-114 extraction was unverifiable because no prior commit existed to diff against (the file was fully untracked, `??`). It has since been committed — but at `8a6cb61`, a single commit that bundles the sibling `evaluation-and-comparison` unit's *creation* of `metrics.py` together with this unit's *edit* of it (same commit message lists `metrics.py`, `bootstrap.py`, `guards.py`, `masks.py`, three units' `code-summary.md` files, and `configs/experiment.yaml` all as one change). `git rev-list --objects --all` confirms only one blob object has ever existed for `src/evaluation/metrics.py`'s path — the pre-extraction 665-line "sibling's READY module" state the current `code-summary.md` (line 22) claims to have modified was never separately committed anywhere, so no diffable baseline exists **and none can now be reconstructed**. The claim that the extraction is "behaviour-preserving" therefore still rests on narrative plus the runtime self-consistency control (`bootstrap.py`'s point-estimate-equality raise, confirmed present and now test-executed green as `test_control_5_point_estimate_inequality_raises`), which — as the prior review already noted — proves internal consistency of the current code, not equivalence to a pre-extraction implementation that left no trace. This is the same substantive gap under a new surface: the commit ledger now exists but was back-filled after the fact across three units at once, so it cannot do the job `team.md`'s own commit-discipline rule (§ Way of Working, Q1/Q2) exists for. | No further git-forensic action can recover the missing baseline; that horse has left. Going forward: when a unit edits a sibling's already-committed module, commit that unit's own work in its own commit (not bundled with the sibling's), so the diff a reviewer needs is mechanically available. For this instance, the mitigating evidence (regression suite green: 60/61 `test_common_masks.py` re-run by this reviewer, 0 failed; the in-function exact-equality control) is the best evidence obtainable and is accepted as sufficient to not block, per the same reasoning the prior review applied — but the underlying gap should not be described as closed. | Reframed, not resolved — commit now exists, but bundles away the only baseline that would have made the diff possible. |
| 2 | Minor | `src/evaluation/bootstrap.py:870-877` vs. `src/evaluation/metrics.py:362-370` (line numbers stable since prior pass, confirmed unchanged: files uncommitted-since-`8a6cb61`, no further commits touch either) | Unchanged from the prior pass: the "no `LockedContext` on a DEC mask" precondition is checked twice (inline in `vector_block_bootstrap`, then again inside `paired_loss_differential`). Harmless defense-in-depth, not a reimplementation of `require_locked_receipt`'s logic. | Same as prior: either drop the inline check or document it as an intentional fail-fast duplicate. | Unresolved, not re-escalated (Minor, non-blocking, unchanged). |

### Independently re-verified this pass (spot checks, not exhaustive)

- **Estimand sign/weighting** (`src/evaluation/metrics.py:282-320`): `se_benchmark - se_model` per masked row (benchmark minus model, positive favours the model when the model's squared error is smaller) → per-station mean → unweighted (`equal_station`) mean across stations, matching Vision §2.3 / TE §1.3 exactly. `configs/experiment.yaml`'s `estimand` block (`orientation: benchmark_minus_model`, `weighting: equal_station`, `sign_convention_sentence`) is asserted byte-identical to `metrics.py`'s `ORIENTATION`/`WEIGHTING`/`SIGN_CONVENTION_SENTENCE` inside `EstimandResult.__post_init__` (`metrics.py:201-213`) — a reversed or drifted sign would raise, not silently pass.
- **Vector time-block bootstrap** (`src/evaluation/bootstrap.py:432-474, 926-956`): `VectorBlockDraw.block_indices` is a single index sequence applied identically to every station inside `_replicate_statistic` (`for station, blocks in block_values.items(): for index in draw.block_indices: drawn.extend(blocks[index])`) — the Q-27 within-station/naive variant is structurally unrepresentable through this shape, not merely avoided by convention. Config declares `block_hours: 24`, `replicates: 10000`, `confidence_level: 0.95`, `seed_key: "seeds.bootstrap"` → `seeds.yaml`'s `bootstrap: 20221201 # D-122`. `pairwise_pearson` (`bootstrap.py:530-575`) computes all three station-pair Pearson correlations on the common masked timestamps and is wired into the mandatory-disclosure path for the widening comparator (R-121).
- **`BootstrapError` single-declaration site**: `grep -rn "class BootstrapError" src/` returns exactly one hit, `src/data/config.py:271`; `bootstrap.py` imports and re-raises it, does not redeclare.
- **D-34 sentinel discipline**: `configs/experiment.yaml:275` — `practical_relevance_threshold: "TBD — freeze gate" # D-34: DECIDED — no threshold approved` — untouched by this unit's diff; nothing in `src/evaluation/bootstrap.py` or `metrics.py` reads or infers this field.
- **D-33 non-interaction**: `cell_rule` (D-33, countersignature not yet given) does not appear anywhere under `src/evaluation/` — this unit's code path is independent of that open freeze.
- **No new TBD fill, no credential, no scientific constant introduced in source**: `20221201`, `24`, `10000`, `0.95`, `48` appear in `src/evaluation/bootstrap.py` only in docstrings/comments (grep-verified), never as an executable literal; all are re-read from `configs/experiment.yaml` / `configs/seeds.yaml` at call time (`test_real_config_confirmatory_shape_rereads_never_literal`, executed and passing in this pass).
- **`code-summary.md` line counts vs. `wc -l`**: `bootstrap.py` 1198, `metrics.py` 731, `test_bootstrap.py` 1121 all match exactly (re-derived, not carried from the prior pass's assertion).
- **No further commits since `8a6cb61` touch `bootstrap.py`, `metrics.py`, `guards.py`, or `test_bootstrap.py`** (`git log --oneline` per file, checked); the three intervening commits (`17e0767`, `8efdfb5`, `f0d9e49`) touch `configs/experiment.yaml`/`configs/data.yaml`/`evidence/DECISIONS.md` for D-33 through D-38 and do not alter the `bootstrap`/`estimand` blocks this unit wrote — confirmed by re-reading both blocks at current HEAD, byte-identical to what `code-summary.md` claims.

### Coverage limits (this pass)

- Same graphify-unavailability and read-scope-bound disclosures as the prior review apply unchanged (no graphify CLI on PATH; no file under `construction/evaluation-and-comparison/` read beyond the named exception).
- Test execution this pass used a reviewer-built ad-hoc runner (stdlib-only, explicitly not pytest), not a real pytest invocation — numpy/pyyaml remain uninstallable in this environment. The runner is disclosed in-line above rather than only asserted.
- The sibling `acquisition` repair mentioned in the dispatch (`_guard_free_text_egress` in `src/data/experiment_registry.py`, new controls in `tests/test_acquisition.py`/`tests/test_clean_run.py`) is outside this unit's files and was not reviewed here — it touches none of `bootstrap.py`/`metrics.py`/`guards.py`/`masks.py`/`test_bootstrap.py`, confirmed by `git status --porcelain` (those paths are absent from this unit's working-tree diff).

### Summary

The unit's estimand sign/weighting, the vector time-block bootstrap's cross-station block-index sharing, the seed/stream discipline, the config transcription, the single-declaration guard reuse, and the D-34/D-33 non-interference all re-verify cleanly against the current repository state, and both test suites this unit touches (`test_bootstrap.py`, `test_common_masks.py`) re-run green with exact counts matching the artifact's claims. The one substantive carry-forward issue — the `metrics.py` extraction's behaviour-preservation claim resting on narrative and a runtime self-consistency check rather than a diffable git baseline — is now unrecoverable rather than merely unaddressed (the pre-extraction state was never committed separately and no longer exists to diff), but this is the same class of gap the prior READY verdict already weighed as Major-not-Critical given the regression suite's green result; nothing about the current state makes it worse, and the regression evidence is, if anything, now confirmed by direct re-execution rather than by count-matching alone. No Critical finding and only one Major (plus one unchanged Minor) survive re-derivation, which is within the READY threshold.

## Floor-reset re-review (2026-09-11)

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-11T14:55:46Z
**Iteration:** 3 (fresh, gate-floor reset; re-derived against current HEAD `b0b7c1d`; adversarial pass, class ADVERSARIAL)

Baseline used for any "unchanged"/"zero diff" claim below: prior verdict at `f0d9e49`/Iteration 2 (2026-09-10). Per `project.md` cid:code-generation:gf-1, this is stated explicitly rather than left implicit.

### Re-derivation method

Environment: no PyPI reachable (verified — egress blocked as before). Ran the two existing reviewer-built ad-hoc runners (`run_bootstrap_tests.py`, `run_common_masks_tests.py`, stdlib-only, explicitly not pytest) at `C:\Users\s_sch\AppData\Local\Temp\22\claude\...\scratchpad\` against CPython 3.11.16 (`uv-pythons/cpython-3.11.16-windows-x86_64-none/python.exe`). Honest naming: this is a reviewer-built substitute for pytest, not a governed pytest run.

- `tests/test_bootstrap.py`: 37 `def test_` functions (`grep -c "^def test_"`, re-derived, matches claim). Ran: **31 passed, 0 failed, 6 skipped** — exact match.
- `tests/test_common_masks.py`: 61 `def test_` functions (re-derived, matches claim). Ran: **60 passed, 0 failed, 1 skipped** — exact match.

### Attack surface re-checked against the seven dispatch items

1. **Estimand sign/weighting** (`src/evaluation/metrics.py:280-320`): `se_benchmark - se_model` per masked row (benchmark minus model; positive favours the model), per-station mean, then `sum(per_station.values()) / len(per_station)` — unweighted (`equal_station`) mean across stations. `configs/experiment.yaml:108-113` transcribes `orientation: "benchmark_minus_model"`, `weighting: "equal_station"`, `sign_convention_sentence` verbatim, matching `metrics.py`'s `ORIENTATION`/`WEIGHTING`/`SIGN_CONVENTION_SENTENCE` (`metrics.py:120-122`), cross-checked byte-for-byte in `EstimandResult.__post_init__` (raises on drift). No reversed or unstated sign found. Confirmed in code, not prose.
2. **Vector time-block bootstrap**: `configs/experiment.yaml:126-136` — `block_hours: 24`, `replicates: 10000`, `confidence_level: 0.95`, `sensitivity_block_hours: 48`, `seed_key: "seeds.bootstrap"` → `configs/seeds.yaml:29` `bootstrap: 20221201 # D-122`, `interval_method: "percentile"`, `block_scheme: "fixed_nonoverlapping"`, `correlation_series: "paired_error_pearson_all_pairs"` — every parameter comes from config, re-read at call time (`read_bootstrap_declaration`), never a source literal (`20221201`/`24`/`10000`/`0.95`/`48` appear in `bootstrap.py` only inside comments/docstrings at lines 90/156/199 — grep-verified, zero executable occurrences). `_replicate_statistic` (`bootstrap.py:444-471`) takes ONE `VectorBlockDraw.block_indices` sequence and applies it identically to every station via the outer `for station, blocks in block_values.items(): for index in draw.block_indices: drawn.extend(blocks[index])` loop — the within-station/naive variant is structurally unrepresentable through this shape, not merely avoided by convention. No alternate/no second bootstrap implementation found anywhere under `src/` or `tests/`.
3. **R-114 one-copy rule**: confirmed still standing exactly as the Iteration-2 review found it. `git log --oneline -- src/evaluation/metrics.py` returns one commit (`8a6cb61`); `git rev-list --objects --all | grep src/evaluation/metrics.py` returns exactly one blob line — no further commits have touched `metrics.py`/`bootstrap.py`/`guards.py`/`tests/test_bootstrap.py` since `8a6cb61` (confirmed again this pass: `git log --oneline` per file shows only `8a6cb61`). The pre-extraction baseline remains permanently unrecoverable; this is the same unresolved-in-substance gap, not a new one, and the mitigating regression evidence (`test_common_masks.py`, 60/61 passed, re-executed this pass) is unchanged. `grep -n "y_true\|y_hats" src/evaluation/bootstrap.py` returns zero hits — the estimand is not reimplemented a second time; `bootstrap.py` reaches it only via `metrics.paired_difference_series`/`equal_station_mean`, with the runtime exact-equality control (`bootstrap.py:908-921`) still present.
4. **`BootstrapError`**: `grep -rn "class BootstrapError" src/` returns exactly one hit, `src/data/config.py:271`; `bootstrap.py:114` imports it (`from src.data.config import BootstrapError, IntegrityError, TBD_SENTINEL`), never redeclares.
5. **48-hour sensitivity**: present and wired — `sensitivity_block_hours: 48` in config, consumed at `bootstrap.py` W-5 (`sensitivity_grid = build_block_grid(..., block_hours=sensitivity_hours, ...)`, drawn on the spawned child stream 0), alongside the 24-hour primary.
6. **`code-summary.md` accuracy — both failure modes checked**: line counts re-derived via `wc -l` this pass: `bootstrap.py` 1198, `metrics.py` 731, `test_bootstrap.py` 1121, `configs/experiment.yaml` 275 (config grew from the claimed 212/275 progression as D-33–D-38 were transcribed by later commits outside this unit — the file table's own count of 212 is stale relative to *today's* 275-line file, but that growth is entirely from sibling/owner commits (`17e0767`, `8efdfb5`, `9d3e853`, `f0d9e49`) transcribing D-33/D-35/D-36/D-38 into the same file, not from anything this unit wrote or needs to re-claim — flagged below as a Minor rather than treated as a misrepresentation of this unit's own work). Test-count claims (37/61 `def test_` functions) re-derived via `grep -c`, both match. No stale "no commit was made" claim exists in this record — the artifact never claims a commit was or wasn't made for this unit's own work; it correctly routes "the governed commit (student's act, none made)" to the open-items list, which remains accurate (`git log` shows no commit citing this unit's D-number was made by the agent).
7. **TBD sentinels / credentials / constants**: `configs/experiment.yaml:275` — `practical_relevance_threshold: "TBD — freeze gate" # D-34: DECIDED — no threshold approved` — untouched by this unit's code; `grep -rn "practical_relevance" src/evaluation/*.py` shows only `diagnostics.py` (a sibling module, outside this unit) reading a *different* symbol (`practical_relevance_statement`), never this threshold. `grep -niE "password|api_key|secret|token"` across `bootstrap.py`/`metrics.py`/`test_bootstrap.py` returns only "identity tokens" prose, no credential. No scientific constant found hidden in source (see point 2).

### Findings

| # | Severity | Location | Finding | Recommendation | Status vs. prior review |
|---|---|---|---|---|---|
| 1 | Major | `src/evaluation/metrics.py`; `git log --oneline -- src/evaluation/metrics.py` (one commit, `8a6cb61`); `git rev-list --objects --all` (one blob) | Unchanged in substance from Iteration 2: the R-114 extraction's behaviour-preservation claim still rests on narrative plus a runtime self-consistency control, never a diffable pre-extraction baseline, because none was ever committed separately and none can now be reconstructed. Re-confirmed this pass: no commit since `8a6cb61` touches `metrics.py`, and the regression suite (`test_common_masks.py`, re-executed: 60 passed / 1 skipped / 0 failed) is the only evidence obtainable. | No further action recoverable on this instance. Carried forward per the prior review's own recommendation: future sibling-module edits should land in their own commit, separate from the sibling's creation commit. | Unresolved, unchanged — same Major, third consecutive pass. |
| 2 | Minor | `src/evaluation/bootstrap.py:869-875` vs. `src/evaluation/metrics.py:362-370` | Unchanged: the "no `LockedContext` on a DEC mask" precondition is still checked twice (inline in `vector_block_bootstrap`, then again inside `paired_loss_differential`). Harmless defense-in-depth, not a reimplementation of `require_locked_receipt`. | Same as prior two passes: drop the inline duplicate or document it as an intentional fail-fast. | Unresolved, not re-escalated (Minor, non-blocking, unchanged for three passes running). |
| 3 | Minor | `code-summary.md` line 20 (`configs/experiment.yaml` file-table row: "212 (was 178)") vs. `wc -l configs/experiment.yaml` = 275 | The file table's line count for `configs/experiment.yaml` (212) is stale against the file's current 275 lines. The growth is entirely attributable to three later commits (`17e0767`, `8efdfb5`, `9d3e853`, `f0d9e49`) transcribing D-33/D-35/D-36/D-38 into the same governed config file after this unit's step closed — not a misrepresentation of what this unit itself wrote, and the `estimand`/`bootstrap` blocks this unit authored are confirmed byte-identical to the claim at current HEAD. Raised as Minor because a reader diffing the table against the file today sees an unexplained mismatch with no note that the delta is sibling/owner-driven. | Add a one-line note to the file table (or the Deviations section) stating that `configs/experiment.yaml`'s line count has grown post-hoc via later D-33–D-38 transcriptions unrelated to this unit's own edit, so the 212-line figure is read as "at time of writing" rather than "at time of review." | New finding this pass (not present in Iterations 1–2, because the later commits postdate them). |

### Independently re-verified this pass

- Estimand sign/weighting: confirmed in code as above (§ Attack surface item 1).
- Vector time-block bootstrap cross-station sharing: confirmed structurally, not by convention (§ Attack surface item 2).
- Seed keyword-only, no default: `bootstrap.py:798-813` — `seed: int` sits after the bare `*`, with no default, alongside other keyword-only parameters that do carry defaults (legal Python: keyword-only parameters may follow defaulted ones); a call omitting `seed` is a `TypeError` by signature, not by a runtime check.
- `BootstrapError` single-declaration site: reconfirmed (§ Attack surface item 4).
- D-34 sentinel and D-33 non-interaction: reconfirmed — `cell_rule` does not appear anywhere under `src/evaluation/` (`grep -rn "cell_rule" src/evaluation/` returns zero hits); D-34's `TBD — freeze gate` sentinel is untouched and unread by this unit's code.
- No forbidden import edges: `bootstrap.py` imports only `src.data.config`, `src.data.splits`, `src.evaluation.guards`, `src.evaluation.metrics` — no `src.features`/`src.models`/`src.external`/`src.gnss` edge; numpy imported lazily, once, inside `_require_numpy`.
- `evidence/DECISIONS.md` confirmed to end at D-38 at current HEAD (matches the dispatch's stated repo-wide context); D-34/D-37 read exactly as the dispatch describes.
- Test execution: both suites re-run fresh this pass (not carried from a prior pass's assertion), exact count match to the artifact's claims.

### Coverage limits (this pass)

- Same graphify-unavailability disclosure as prior passes: no `graphify` executable on PATH (re-verified this pass via `which graphify`); oriented by direct file reads throughout, per the dispatch's own instruction and the project's documented exception for this clone.
- Per the reviewer read-scope bound, no file under any sibling unit's `construction/<unit>/` record dir was read; the one workspace-code touchpoint outside this unit's own files (`src/evaluation/diagnostics.py`, checked only to confirm `practical_relevance_statement` is a distinct symbol from the D-34 config field) is a spot-check on a named integration point, not a sweep of a sibling's record dir.
- Test execution used the reviewer-built stdlib-only ad-hoc runners already present in the scratchpad from the prior pass, not a real pytest invocation; numpy/pyyaml remain uninstallable in this environment (PyPI egress blocked, re-verified).
- The R-114 behaviour-preservation gap (Finding 1) remains permanently unverifiable by diff; this pass adds no new evidence beyond re-confirming the commit-history facts and re-running the regression suite.

### Summary

Every item in the dispatch's specific attack list re-verifies cleanly against the current repository state and current HEAD (`b0b7c1d`): the estimand's sign convention and equal-station weighting are correct and drift-guarded in code; the vector time-block bootstrap shares one block-index draw across all three stations by construction, foreclosing the within-station/naive substitute; all scientific parameters are config-sourced and re-read, never literal; `BootstrapError` is not redeclared; the 48-hour sensitivity exists alongside the 24-hour primary; both owned test suites re-run green with exact counts matching the record's claims (31/0/6 and 60/0/1); no TBD sentinel was filled, no credential found, no scientific constant hidden in source. The one Major (R-114's unrecoverable pre-extraction baseline) is unchanged in substance across three review passes and was already weighed as Major-not-Critical given the standing green regression evidence; the one Minor (duplicate guard) is unchanged and non-blocking. One new Minor is raised this pass — a stale line-count in the artifact's own file table, caused by later sibling/owner commits growing the same shared config file after this unit's step closed, not by anything this unit misrepresented about its own work. Zero Critical, one Major, two Minor: within the READY threshold.

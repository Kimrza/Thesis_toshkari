# Code Summary — `models-and-baselines`

**Unit** `models-and-baselines` (Bolt 8) · **Kind** `library` · **Stage** `code-generation`
**Plan**: `code-generation-plan.md` — Steps 1–6 and 8–11 executed and ticked; **Step 7 left unticked by its own gate** (no D-number reopening D-27 exists on disk — see § Step 7 outcome). This stage made no `git commit`; see § Repository state for the owner commits that landed during the pass.
**Rulings built under** (receipted in `code-generation-questions.md`): Q1 = A (BLK-03 contract approved), Q2 = B superseded by FU-1 = C (M-06 Keras code behind a pin guard; TensorFlow pin stays `TBD — freeze gate`), Q3 = A (`scikit-learn==1.4.2` pinned), Q4 = B superseded by FU-2 = B (owner reopens D-27 by a new D-number first; the inverse step gated on it), Q5 = A (D-121 grids and Vision §8.6's seven fixed LSTM settings transcribed). The change record recording all of them was written FIRST, before any module.

## Files created

| Path | What |
|---|---|
| `governance/CHANGE_RECORD_2026-09-06_BLK03_confirmatory_contract.md` | Record `CR-2026-09-06-BLK03-CONFIRMATORY-CONTRACT` (353 lines): owner approval of the confirmatory-prediction contract (`domain-entities.md` § 3's four limbs, the five-point consumption contract, R-91/R-92/R-93) as the governed BLK-03 contract; the `scikit-learn==1.4.2` engineering pin; the FU-1 = C posture; the D-121 / Vision §8.6 / TE §7.2 transcription; FU-2 = B with a section headed **"Proposed decision text for the owner to adopt (NOT a decision)"** drafting the D-number that would reopen D-27 — the owner writes it into `evidence/DECISIONS.md`, this stage did not. Blocker-register annotations routed to the gate; propagation-sweep table; owed list; commit citations D-121, D-122, D-27, D-31 |
| `src/models/train.py` | `Prediction` (approved 8 fields); `read_horizons` / `resolve_horizon` / `eligible_feature_columns` (R-99, horizon config-only); **`assert_stamp_match`** (R-90's three checks: `PartitionError` on partition or role mismatch, `LeakageError` on `transform_id` `None` or foreign); **`fit_predict(model_id, *, bundle, partition, snapshot, …)`** (approved signature plus additive keywords; `LeakageError` when `bundle.transform_id is None`; closed model set); **`three_seed_mean(predictions, *, expected_seeds)`** enforcing all four limbs (`SeedError` incl. count, `seed is None`, non-M-06, wrong-but-distinct triple; `AlignmentError` on the ordered (`station`, `interval_start_utc`) index as a set and in order; `PartitionError` on cross-partition or training partition; `LeakageError` on `transform_id` disagreement or `None`; provenance copied; `seed = None` on the mean); `TuningRecord` (seven approved fields plus `attested_by`, `attested_at_utc`, `attests_criterion_hash`) and `record_tuning` (R-95: December in `partitions_read` refuses, `criterion_hash != criterion_used_hash` refuses, missing attestation refuses — SD-M-01 unconditional); `read_grids` / `enumerate_grid` / `assert_grid_content` / `assert_in_grid` / `grid_hash` / `assert_grid_unchanged` / `read_lstm_fixed_settings` (R-96: content asserted as enumerated product == D-121's declared `combinations`, both from config); `CandidateScore` / `mean_per_fold_skill` / `select_configuration` / `assert_refit_unchanged` (R-101); `AblationEntry` / `read_ablations` / `assert_ablation_runnable` / `assert_no_promotion` (R-97: five named from config; `ABL-HIST48` refuses before the primary freeze; `ABL-DIFF` refuses naming D-27 and `InverseTransformError`; `ABL-ZENITH` refuses as Phase 2); `PredictionHashReceipt` + `write_prediction_hash_receipt` (`.tmp` → fsync → `os.replace`) / `load_prediction_hash_receipt` / `assert_receipt_matches` / `assert_locked_exit_allowed` (R-102a, SD-M-04); `pinned_requirement` helper |
| `src/models/persistence.py` | M-01 persistence and M-02 24-hour seasonal persistence (`seasonal_lag_hours` from config, `persistence_rows`, `fit_predict_rows`); read the raw-TECU target series, never standardised lag columns |
| `src/models/climatology.py` | M-03 station×month×hour climatology: `fit_climatology` refuses rows outside the training range (`LeakageError`), `predict_rows`, `climatology_fit_partition`, `assert_fitted_on_training_partitions`, `FittedPartitionRecord` |
| `src/models/ridge.py` | M-04 Ridge: `require_sklearn` lazy import refusing by the `requirements.txt` pin's name; `fit_predict_rows` asserting grid membership against config |
| `src/models/random_forest.py` | M-05 direct Random Forest: `ImportanceFigure` whose `authoritative=True` is unrepresentable, `diagnostic_importance` the only producer (R-100); `random_state` = `seeds.development` from config; `Prediction.seed` stays `None` (not a D-122-seeded family) |
| `src/models/checkpoint.py` | `EpochRecord`, `Checkpoint` (6 attributes), `CheckpointBackend` protocol, `select_best` (lowest validation RMSE; tie → earliest; NaN refused), `restore`, `assert_restored_is_best` (last-epoch restore fails — R-94), `should_stop`, `read_checkpoint_policy` |
| `src/models/lstm.py` | M-06 compact LSTM: **`require_frozen_pin`** — every `tensorflow` import sits behind it (three guarded sites, none at module scope) and the real `requirements.txt` refuses; `enumerate_lstm_grid` (16 from config), `fixed_settings` (seven from config), `observable_steps`, `build_keras_model` (tf.keras 2.21.0 candidate API, unexecuted), `determinism_check` (SD-M-05 positive probe; degradation recorded when no probe subject), `fit_predict_rows` with refusal order grid → settings → seed → backend → pin guard, before any import |
| `scripts/06_train_and_predict.py` | Position 06; six-step entry (`ensure_process_determinism` first; `_assert_phase1_field_contract` = `assert_no_raw_fields` before the first write, passing the R-24 checker); `_assert_registry_column_18`; horizons and grid content asserted; inputs by manifest (refuses today at the released-target manifest / `split_manifest.json`); `assert_stamp_match` before every scoring path; M-01…M-05 once and M-06 per configured final seed; `three_seed_mean(expected_seeds=_final_seeds(snapshot))` read at the call site; `--partition DEC` excluded from the default list and requiring `--g05-signature --locked-input --locked-authorization`; the locked path enters only through `materialise_locked_partition`, its loader routing the human-supplied path through `open_restricted` with `purpose="locked_evaluation"`; `_finish_locked_write` (write once → receipt → registry append → `assert_locked_exit_allowed`, `LockedTestError` on refusal to exit); child registry rows per (partition, model, seed); honest `aborted` row on every `IntegrityError`; `prior_period_exposure` never written; the restricted root never named |
| `tests/test_models_smoke.py` | §12-mandated. 56 test functions (54 at iteration 1, +2 at iteration 2 — the `DEC` target-wiring test and its refusal control, § Iteration 2): closed model set and AST absence scans (residual/GRU/PyTorch); M-01/M-02/M-03 paths and the R-98 training-only control; M-04/M-05 refusal by name and grid-content controls over synthetic grids; R-90's four controls with control 3 enumerated over ordered id pairs and the must-not-fire case; `three_seed_mean` pass case and every limb-4 control (single seed, best-of-three, wrong-but-distinct triple constructed at test time, `seed None`, non-M-06, one-row and order misalignment, cross-partition, training partition, `transform_id` disagreement / `None`); no three-integer literal set in `src/models`; tuning (December, criterion hash, attestation, ordering); selection; ablations (five / missing / unregistered / late / `HIST48` / `DIFF` naming D-27 / `ZENITH` / no-promotion); horizon config-only with a static literal-branch scan; RF importance marker and module-graph check; pin guard (real file refuses, commented pin refuses, frozen tmp pin passes; `tensorflow` never in `sys.modules`); seven settings; receipt controls through `06`'s functions on a synthetic non-`DEC` fixture (no receipt / hash mismatch / failed append / raising append / write-once); column 18; `DEC` guard refusals |
| `tests/test_checkpoint_restore.py` | §12-mandated. 12 test functions: lowest-RMSE selection, tie → earliest, NaN / negative / duplicate / empty refused, fake-backend round trip, last-epoch restore fails, foreign payload fails, six attributes, early stopping with synthetic patience / tolerance, policy from config with `last_epoch` refused, each of the eight setting keys required |

## Files modified in place

| Path | What |
|---|---|
| `requirements.txt` | `scikit-learn==1.4.2` under the scientific-base block with a comment citing the change record (Q3 = A). TensorFlow stays comment-only `TBD — freeze gate` |
| `configs/experiment.yaml` | `grids` transcribed from Vision §8.6's table (`PreFlight/vision_document(3)(2)(2).md` lines 809–822) and §14.2 row D-121 (line 1206): Ridge `alpha` six values / 6; RF `n_estimators` × `max_depth` (`None` → YAML `null`) × `min_samples_leaf`, `max_features: sqrt` / 18; LSTM `layers` × `units` × `learning_rate` × `batch_size` / 16 — each with `decision: "D-121"` and `combinations`. `models.lstm_fixed_settings`: the seven §8.6 settings with the §8.6 sentence quoted verbatim in `source_text`. `ablations.entries`: TE §7.2's five named runs (`Technical_Environment_and_Research_Implementation(1)(2).md` lines 404–420), `run_id` / `registered_at` kept `TBD — freeze gate`. Sentinel-only SHAPE keys added: `models.declared_baseline_per_track`, `models.selection`, `models.selected` (all `TBD — freeze gate`). **Not transcribed, deliberately**: `horizons`, D-124's tolerance / baseline, per-ablation parameters — outside Q5 = A; the code refuses naming each |
| `src/data/config.py` | `REQUIRED_FIELDS_MAP[("models-and-baselines", 1)] = ("seeds.development",)`, minimal, same comment discipline as the four sibling entries |
| `evidence/test_run_access_log.jsonl` | +37 rows (3444 → 3481) appended by running the EXISTING suite (routed restricted reads the existing tests log by design). Not reverted — access records are never deleted; flagged for the gate |
| `code-generation-plan.md` | Ten checkboxes ticked (Steps 1–6, 8–11); Step 7 left unticked by its gate; no other line changed |

## Step 7 outcome — gate ABSENT, nothing built

The developer checked `evidence/DECISIONS.md` on reaching Step 7: the last headings are `D-31` (line 1626), `D-32` (line 1699) and a `D-1 addendum` (line 1779); zero entries dated 2026-09-06 or later; the only "reopen" hits are D-2's and D-3's. No D-number reopens D-27. Per the plan, the step stopped: `src/features/transforms.py` was **not touched**, no inverse exists, `ABL-DIFF` refuses naming D-27 (`assert_ablation_runnable`, tested). The proposed decision text sits in the change record for the owner to adopt or discard; if adopted later, Step 7 is a bounded follow-up (additive method on `Transform`, its two negative controls, `ABL-DIFF` runnable), not a re-run of this pass.

## Test and lint results (smoke evidence only — never governed)

- **Interpreter**: uv-managed Python 3.11.16. pandas, numpy, pyyaml, scikit-learn, tensorflow, pytest and ruff all absent; `uv pip install scikit-learn==1.4.2 pyyaml==6.0.1` into the scratchpad venv timed out against pypi.org. Nothing installed into the repo.
- **Owned modules under the stdlib pytest stand-in**: `test_models_smoke` 53 passed / 1 skipped at iteration 1 (the yaml-gated real-config transcription test), **55 passed / 1 skipped after iteration 2**; `test_checkpoint_restore` 12 passed.
- **Whole suite under the stand-in** (22 modules): measured **877 passed / 38 failed / 5 skipped**. 37 failures are the known environmental set (`test_determinism` 22 and `test_external_drivers` 12 need `yaml`; 2 need `pytest.approx`; 1 needs `capsys`). The 38th was this pass's own: a new test spelled the restricted-root literal and R-28's one-door AST scan in `test_locked_test_guard.py` caught it — fixed by comparing against the imported `RESTRICTED_ROOT`; the targeted re-run of the five modules containing that failure gives 151 passed / 0 failed / 1 skipped. **Projected** whole-suite figure after the fix: 878 / 37 / 5 (not re-measured in full, because every whole-suite run appends 37 rows to the governed access log). Yesterday's baseline 813 / 37 / 4 was counted by the same stand-in; real pytest has never run here.
- **Independent check (orchestrator)**: `grep -c '^def test_'` gives 56 / 12 after iteration 2 (54 / 12 at iteration 1); no `1337`, `2024`, `20221201` or grid-value literal in `src/models/*.py` or the two test files; `tensorflow` appears only at three guarded sites inside `lstm.py` functions, `sklearn` only at three lazy sites inside fit paths; no `src.external.iri` / `gim`, `src.evaluation`, or `torch` import under `src/models`; plan ticks 10 with Step 7 the one unticked line.
- **ruff was NOT run** (not installable). The stdlib substitute reports 0 problems over the 11 touched files (line length ≤ 99, trailing whitespace, unused imports); `ruff check` / `ruff format --check` owed. Git warns `requirements.txt` carries CRLF that will be normalised.

## Key decisions

1. **`Prediction` lives in `train.py`** and the family modules are imported lazily inside `fit_predict`, so W-11's ten-file list holds with no private shared module (no second `_frames.py`-style naming amendment).
2. **`fit_predict` keeps the approved shape and adds keyword-only `target`, `score_bundle`, `seed`, `params`, `horizon_hours`**: the bundle carries no label and its lag columns are standardised under `transform_id` with no inverse (D-27), so labels and the raw-TECU series come from the D-17 target frame; fitted families fit on the train bundle and predict on the score bundle. Recorded as an additive amendment candidate against `component-methods.md`.
3. **`assert_stamp_match`'s "that partition's own transform" is `T-<id>`**, and for `DEC` the refit's `T-REFIT` (R-74's single enumerated `REFIT`→`DEC` apply); a `DEC` bundle carrying `T-DEC` raises `LeakageError`.
4. **R-95's tuning refusals raise `LeakageError`** (December informing selection is the leakage channel R-95 closes); ablation refusals use `InverseTransformError` for `ABL-DIFF` and `PhaseBoundaryError` for `ABL-ZENITH`; no exception redefined.
5. **Grid content** is asserted as "enumerated product of the transcribed axes == D-121's declared `combinations`", both from config; the frozen object is the block's SHA-256; tests use synthetic grids so no D-121 value is a test literal.
6. **The seven settings** are asserted present and typed from config, `checkpoint_policy` compared to the identity token `best_checkpoint`; `Adam` / `MSE` identities map to Keras names inside `lstm.py`.
7. **`06` reads M-04…M-06's grid point from `models.selected.<track>` and refuses while unset** — a grid point picked in the script would be a selection with no record (R-101).
8. Docstrings avoid the tokens of the two removed architectures and the prohibited stack so TA-12's grep evidence stays literal-clean; a test enforces it.

## Deviations

- **Approved-signature deviations (additive / keyword-only), amendment candidates**: `fit_predict(..., target=None, score_bundle=None, seed=None, params=None, horizon_hours=None)`; `assert_stamp_match(..., own_transform_id=None)`; `climatology_fit_partition` reads the fitting record from the prediction frame's `attrs` (approved `Prediction` shape unchanged — the declined ninth amendment).
- **Config SHAPE added beyond the three transcribed blocks**: `models.declared_baseline_per_track`, `models.selection`, `models.selected` as `TBD — freeze gate` sentinels (W-9 precedent). No value written.
- Test execution through the stdlib stand-in, no ruff run — environmental, stated above.
- The developer's dispatch was interrupted once by an API rate limit after reading the artifacts and before writing anything (verified on disk); the same agent resumed from its transcript under the unchanged plan.

## Design observations for the gate (not settled here)

- **M-03 under the frozen fold layout**: a station×month×hour climatology fitted on an expanding fold's training range has no key for the validation month (F1: January–March training → April has no month key); the code emits `y_hat = None` and counts `missing_climatology_keys` rather than filling. Whether M-03 is a viable difficulty control under the frozen folds is a scientific question for the owner, not an implementation choice.
- **Tuning orchestration home**: `06` carries no grid search over F1–F4; `record_tuning` / `select_configuration` exist in `train.py` and `06` consumes `models.selected`. Where the tuning pass lives is owed.
- **Locked-input format**: `06`'s locked loader reads `.jsonl` / `.csv` after `open_restricted`; the December artifact's real on-disk format is unknown to this pass.
- **Sweep finding outside this unit**: `configs/seeds.yaml`'s header still quotes D-122 as "supervisor sign-off pending at G-05" (stale since 2026-08-22 per Vision §14.2 line 1207); reported, not edited.

## Repository state — owner commits during this pass

Derived from `git log` on 2026-09-06, not from prose: HEAD is **`da6cb7b`**; `ec8eacf` (15:43:40 +04:00) and `da6cb7b` landed while this unit's questions, plan and code were being produced, both by the repository owner with git's unedited template placeholder as message and no D-number cited, and the working tree now shows this pass's files as committed. Same disposition question as `6246907` (recorded at `features-and-splits`): amend / redo citing the D-numbers the change record names, or let stand with a reason. Nothing here reverts, amends or re-commits.

## Governance stop — owed before any commit (student acts; cumulative with prior units)

- **Step 11 items**: BLK-03 register annotation in `unit-of-work.md` (contract approved; evidence limb open at G-05); the owner's decision on the proposed D-number reopening D-27 (Step 7 absent today); the TensorFlow pin (unfrozen; M-06's Keras path written but unexecutable anywhere; TA-26 `Pending`); `scikit-learn==1.4.2` install evidence; the `pyarrow` pin carried from features-and-splits; `requirements.md` FR-P1-05-2's superseded bootstrap-seed attribution (raised by `domain-entities.md` § 3, not edited); the `prior_period_exposure` deviation from the approved remediation text (R-102a); the `seeds.yaml` header quote; the additive-signature amendment candidates; the three sentinel keys under `models`; `horizons` / D-124 transcription owed to their owners; the M-03 fold / month-key observation; the tuning-orchestration home; the 37 appended access-log rows.
- Governed commit (student's act) citing **D-121, D-122, D-27, D-31**, plus the new D-number if adopted — and the standing disposition of the owner's template-message commits (`6246907`, `ec8eacf`, `da6cb7b`).
- `pytest -rs`, `ruff check`, `ruff format --check`, and the yaml-gated real-config transcription test the first time the pins are installable.
- **Nothing discharged**: WS-14, WS-15, TA-12, TA-13, TA-26 stay `Pending`; the seven rowless requirements stay rowless. No model was trained; M-04 / M-05 fits unrun; M-06's Keras code unexecuted; the `DEC` path unreachable behind the G-05 guard; no December content, restricted-root path or real signature touched by any new test.

## Iteration 2 — the iteration-1 Critical fixed (2026-09-06, same session)

The reviewer's iteration-1 section below is left standing; its NOT-READY verdict predates this change.

- **Critical (the `DEC` iteration discarded the frame `materialise_locked_partition` returned and would have scored December against the pre-loop released target)** — fixed in `scripts/06_train_and_predict.py` (`git diff --numstat` vs HEAD: 70 insertions / 5 deletions; 741 → 794 lines). New helper `_locked_target(snapshot, *, g05_signature, loader, partitions, released_target)`: wraps the caller's loader in a guard that raises `LockedTestError` if the loader hands back the pre-loop released-target object (checked by identity on the loader's OUTPUT, before the door's membership and embargo steps), calls `materialise_locked_partition` — still the one door, still refusing first on `None` or a non-verifying signature — and returns the frame the door returned (locked month, embargo excluded and counted, `attrs["partition_id"] == "DEC"`), refusing a `None` return or a returned released target. `_run` binds `released_target` before the loop; on the `DEC` iteration `partition_target = _locked_target(...)`, on every other partition `partition_target = released_target`; the single `fit_predict(...)` call reads `target=partition_target` (grep: `target=partition_target` 1, `target=target` 0). Refusal order and the receipt sequence in `_finish_locked_write` unchanged; docstring states the mechanism.
- **Two tests added** to `tests/test_models_smoke.py` (+141 lines; 54 → 56 functions), over the synthetic year with a synthetic `gates.G-05` record and a fake loader: `test_the_dec_iteration_scores_the_frame_the_one_door_returned_not_the_released_target` (loader called exactly once for `DEC`; returned frame `is not` the released target; `partition_id == "DEC"`; embargo rows counted; every loaded value carries the fixture offset and none of its keys exists in the released January–November target; an M-01 `DEC` score bundle carrying `T-REFIT` yields `y_hat` from the loaded series with zero missing, while the same scoring against the pre-loop target yields `None` for every row) and `test_a_dec_iteration_handed_the_pre_loop_target_is_refused` (loader returning the released target → `LockedTestError` naming it; non-verifying signature → `LockedTestError` before the loader runs; `g05_signature=None` → `LockedTestError`; the script text contains `target=partition_target` and no `target=target`). No December 2022 content, restricted-root literal or real signature in either.
- Re-verified under the stand-in (smoke only): `test_models_smoke` 55 passed / 1 skipped; `test_checkpoint_restore` 12; `test_locked_test_guard` 44 (literal scan still passes); `test_phase_contract` 36 (R-24's guard-before-first-write checker still satisfied by `06`). `py_compile` OK for both files; stdlib lint substitute 0 problems. `git log -1` still `da6cb7b`; no commit. Plan checkboxes unchanged.
- Riding suggestions (a note at `LSTM_FIXED_SETTING_KEYS` explaining 8 keys for 7 settings; the duplicate `assert_stamp_match` call in `06`) NOT applied — gate input, quoted at the gate.

## Review — 2026-09-06 (code-generation, iteration 1)

**Verdict:** NOT-READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-06T14:15:56Z
**Iteration:** 1

### Findings

| # | Severity | Location | Finding | Recommendation |
|---|---|---|---|---|
| 1 | Critical | `scripts/06_train_and_predict.py:606-636` | The one-shot `DEC` write path — the mechanism this unit's own Step 8/R-102a/SD-M-04 exist to build — routes December's target through the one-door `materialise_locked_partition(...)` / `open_restricted` at lines 608-613, but **discards its return value**: no name captures it. The loop then falls through to `train_bundle, score_bundle = _bundle_pair(...)` and, at line 631, calls `fit_predict(..., target=target, ...)` where `target` is the single `_load_target_by_manifest(snapshot)` object loaded once at line 600 **before the partition loop**, and reused for every partition including `DEC`. The restricted December frame that `open_restricted` legitimately reads, logs and returns is therefore never consulted for labels: M-06's three seeded fits for `DEC` would look up `(station, t)` in the non-December `target` series (which, if the released Phase 1 target excludes the locked month as intended, has no December keys at all, producing an all-`None` confirmatory prediction; if it does carry December, the restricted-read/access-log/receipt apparatus around it is theater, since the value it fetched is never used). The receipt `write_prediction_hash_receipt` then hashes and durably commits a prediction file computed from the **wrong** source, while the `AccessRecord` written by `open_restricted` truthfully asserts a `locked_evaluation` access that had no bearing on what got predicted or receipted — an audit-trail/actual-computation mismatch at exactly the boundary R-102a/SD-M-04/W-12 exist to make airtight. This is dead code today only because `_load_target_by_manifest` (line 600) unconditionally raises before the partition loop is ever entered (verified: the function's second `raise IntegrityError` at line 327 fires unconditionally once the manifest exists) — so the bug is latent, not currently triggerable — but it ships inside code the summary calls "the one-shot `DEC` write path implemented in full," which is not true of its target-wiring. No test exercises this: `tests/test_models_smoke.py::test_the_dec_path_is_unreachable_without_the_g05_guard_and_the_three_arguments` (the only test that touches `materialise_locked_partition`) only asserts the `g05_signature=None` refusal — it never calls the function with a verifying signature and checks that its returned frame reaches `fit_predict`/`three_seed_mean` as the December label source. | Capture `materialise_locked_partition`'s return (e.g. `locked_target = materialise_locked_partition(...)`) and pass it as `target=locked_target` to `fit_predict` for the `DEC` iteration only (keeping `target` for every other partition), then add a test that supplies a verifying `g05_signature`/fake loader and asserts the loaded frame — not the pre-loop `target` — is what the December scoring calls receive. |

### Verified — did not break

- `Prediction` (8 fields), `fit_predict(model_id, *, bundle, partition, snapshot, …)` and `three_seed_mean(predictions, *, expected_seeds)` match `component-methods.md` and `domain-entities.md` § 2/§ 3 exactly; every deviation (`target`, `score_bundle`, `seed`, `params`, `horizon_hours` on `fit_predict`; `own_transform_id` on `assert_stamp_match`) is additive/keyword-only and listed in § Deviations. `ConfigSnapshot` untouched.
- `three_seed_mean`'s all four limbs (`SeedError`/`AlignmentError`/`PartitionError`/`LeakageError`) match `domain-entities.md` § 3 limb 4 and `business-rules.md` R-91/R-92/R-93; `expected_seeds` is read at the call site in `scripts/06_train_and_predict.py:493-505` and `grep`-confirmed never inlined in `src/models` or tests (no `1337`/`2024`/`20221201` literal anywhere checked); the alignment key is `(station, interval_start_utc)`, checked as a set and in order (`train.py:780-796`).
- `assert_stamp_match`'s three checks and R-90's four negative controls plus the must-not-fire control reproduce `business-rules.md` R-90 exactly, including control 3 by enumeration (`test_control_3_by_enumeration_over_ordered_pairs_of_the_six_ids`, asserts `25` raised, re-derived: 5 scorable ids incl. `DEC` × 6 minus 5 diagonal matches = 25) and the `DEC`→`T-REFIT` (`REFIT`→`DEC`) reading, consistent with `src/features/transforms.py`'s untouched state and R-74's enumerated exception.
- `TuningRecord`'s 7 approved + 3 attestation fields, December refusal, criterion-hash equality and unconditional attestation (SD-M-01) all present and tested (`record_tuning`, `train.py:857-940`).
- `configs/experiment.yaml`'s grid/settings/ablation transcription verified verbatim against `PreFlight/vision_document(3)(2)(2).md` lines 809-822/1206 and `PreFlight/Technical_Environment_and_Research_Implementation(1)(2).md` lines 404-420 (diffed by hand); counts 6/18/16 correct; `None`→`null` correct; `test_real_experiment_yaml_transcription_is_internally_consistent` re-reads counts from config, asserts no literal.
- `tensorflow` imports (`src/models/lstm.py:175,203,291`) all sit strictly inside functions that call `require_frozen_pin()` first; no module-scope import; `sklearn` imported lazily at 2 sites (`ridge.py:123`, `random_forest.py:158`) behind `require_sklearn`; `requirements.txt` carries `scikit-learn==1.4.2` and only a comment for TensorFlow — confirmed by direct read.
- No import of `src.external.iri`/`gim`, `src.evaluation`, or `torch` anywhere under `src/models` or `scripts/06_train_and_predict.py` (grep-confirmed); no residual/GRU module exists; `PartitionError` imported from `src.data.config` in `train.py`, matching the 2026-08-28 declaration-site ruling.
- Step 7 gate honestly honored: `evidence/DECISIONS.md`'s last headings are D-30/D-31/D-32 (verified by direct read), no entry reopens D-27; `src/features/transforms.py` carries no `inverse` (grep-confirmed) and is untouched by this pass's commits (`git diff HEAD~3 -- src/features/transforms.py` shows the file's original addition only, no further edit); `ABL-DIFF` refuses naming D-27 in both `train.py:1348-1355` and its test.
- Counts independently derived: `grep -c '^def test_'` gives exactly 54 (`test_models_smoke.py`) and 12 (`test_checkpoint_restore.py`), matching the claimed figures; plan shows 10 ticked boxes, Step 7 unticked, matching the claim; `evidence/test_run_access_log.jsonl` is 3481 lines, matching "+37 rows (3444 → 3481)"; `requirements.txt` gained exactly the claimed `scikit-learn==1.4.2` line under a governed comment.
- `py_compile` succeeds on all 11 touched Python files (uv-managed Python 3.11.16). Re-ran the two owned modules under the stdlib pytest stand-in: **65 passed, 0 failed, 1 skipped** (54+12−1, matching the claimed 53+12), reproducing the summary's result. Re-ran `test_locked_test_guard.py` and `test_phase_contract.py` (the two modules this unit could have broken, per dispatch) under the same stand-in: **80 passed, 0 failed** — no regression. `evidence/test_run_access_log.jsonl` line count unchanged after these runs (both modules route through `tmp_path` registries, not the governed log).
- `git log` confirms HEAD `da6cb7b`, with `ec8eacf` and `da6cb7b` both carrying git's unedited template placeholder message and no D-number — matches § Repository state's description exactly; this stage made no commit of its own (verified: no new commit atop `da6cb7b`).
- `evidence/DECISIONS.md` untouched by this stage (only the pre-existing D-1…D-32 entries present); the change record's § "Proposed decision text for the owner to adopt (NOT a decision)" is clearly labelled as such.

### Coverage limits

- Did not independently re-verify every one of `test_models_smoke.py`'s ~54 assertions line-by-line against every cited business rule (R-95 mechanism 3's `audit_access_since_declaration` wiring, R-101's tolerance/complexity ordering, and the M-03 fold/month-key design observation were read but not separately fixture-traced).
- Did not execute `src/data/experiment_registry.py`'s full write path end-to-end (only via the two re-run test modules and the smoke suite's own coverage); relied on the stdlib pytest stand-in throughout, as the dispatch specifies — never governed evidence.
- Did not read sibling units' `construction/<other-unit>/` content per the read-scope bound; cross-unit claims (R-105/R-92 exception-type reconciliation, BLK-04/BLK-09 approval) were taken from the shared `business-rules.md`/`domain-entities.md` text already in scope, not independently verified against `evaluation-and-comparison`'s or `features-and-splits`' own artifacts.
- Did not attempt to install `scikit-learn`/`tensorflow`/`pyyaml` (PyPI confirmed unreachable per the dispatch); M-04/M-05/M-06 fit-path arithmetic (grid search correctness, Keras model construction, checkpoint save/load against a real backend) is therefore verified only by structural/refusal tests, never by a real fit, matching the artifact's own stated limits.

## Review — 2026-09-06 (code-generation, iteration 2)

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-06T14:15:56Z
**Iteration:** 2

### Findings

| # | Severity | Location | Finding | Recommendation |
|---|---|---|---|---|
| 1 | Minor | `code-summary.md` § Iteration 2, bullet 1 | Claims `scripts/06_train_and_predict.py` grew "741 → 794 lines," but `wc -l` gives **806** lines, and the same sentence's own numstat (70 insertions / 5 deletions) arithmetically requires 741+70−5=806, not 794 — the stated total contradicts the stated diff in the same breath. `project.md` requires a count be derived from the artifact and printed before assertion; this one wasn't. | Correct "794" to "806" (or drop the redundant total and keep only the numstat, which is itself correct). |

### Verified — did not break

- **Traced `_run`'s `DEC` branch end to end** (`scripts/06_train_and_predict.py:657-696`): `released_target` is bound once, pre-loop, from `_load_target_by_manifest`. On `pid == LOCKED_ID`, `partition_target = _locked_target(snapshot, g05_signature=..., loader=_locked_loader(...), partitions=partitions, released_target=released_target)`; every other partition takes `partition_target = released_target`. The single `fit_predict(...)` call in the seed loop reads `target=partition_target`. Grep-confirmed: exactly one `target=partition_target` in the file, zero occurrences of `target=target`. No path exists from `released_target` into `DEC`'s `fit_predict`/`three_seed_mean`/receipt calls.
- **The identity guard fires on the loader's raw output, before membership/embargo**: `_locked_target`'s `_guarded_loader` wraps the caller's loader and raises `LockedTestError` on `frame is released_target` immediately after `frame = loader(partition)` returns — this wrapped callable is what `materialise_locked_partition` invokes as `loader(locked)` *before* its own `assert_membership_from_timestamps` and `apply_embargo` calls (`src/data/splits.py`, unchanged, confirmed via `git diff HEAD -- src/data/splits.py` showing no diff). A second identity check on `_locked_target`'s own return value (post-embargo) is a harmless, redundant belt-and-suspenders check.
- **Refusal order preserved**: `materialise_locked_partition` itself is untouched (`None` signature → `LockedTestError` before any read; non-verifying signature → `LockedTestError` before the loader runs — both reconfirmed by `test_a_dec_iteration_handed_the_pre_loop_target_is_refused`, which shows the loader is never called on either failure path since the fake loader would otherwise return a distinguishable frame). `_finish_locked_write` (write-once → receipt `.tmp`→fsync→rename → registry append → `assert_locked_exit_allowed`) is **byte-identical** to iteration 1 (confirmed by direct read of both versions).
- **The two new tests assert what the summary claims**, verified by direct read: `test_the_dec_iteration_scores_the_frame_the_one_door_returned_not_the_released_target` asserts `loader_calls == [LOCKED_ID]` (called once), `loaded is not released_target`, `loaded.attrs["partition_id"] == LOCKED_ID`, `excluded_embargo_rows == locked.embargo_hours * len(STATIONS)`, every loaded value carries `SYNTH_LOCKED_OFFSET` and none of its keys exists in the released target's series, `fit_predict(target=loaded)` yields the offset value with zero missing while `fit_predict(target=released_target)` on the identical bundle yields `None` for every row. `test_a_dec_iteration_handed_the_pre_loop_target_is_refused` covers all three refusals (loader-returns-released-target by identity; non-verifying signature; `None` signature) plus the text-scan for `target=partition_target` present / `target=target` absent. Both use a synthetic year, a synthetic `gates.G-05` signature (`_signed_snapshot`, hashed locally, no real `configs/data.yaml` gate touched), and no restricted-root literal (grep-confirmed against `src.data.locked_test.RESTRICTED_ROOT` in the pre-existing test at the same module, unaffected by this diff).
- **Re-ran under the shim**: `py_compile` clean on both changed files. `test_models_smoke`: **55 passed, 0 failed, 1 skipped** (56 functions − 1 yaml-gated skip), matching the claim exactly. `test_locked_test_guard` + `test_phase_contract` + `test_checkpoint_restore` together: **92 passed, 0 failed** — reconciles with the claimed 44/36/12 (function-def counts are 34/23/12; the parametrized-case totals the shim reports sum to 44+36+12=92, matching); this exactly reproduces iteration 1's 80 (guard+phase_contract) plus checkpoint_restore's unchanged 12, confirming no regression. `evidence/test_run_access_log.jsonl` remains 3481 lines (unchanged) — did not run the whole suite.
- **Numstat and 56-count reconciled**: `git diff --numstat HEAD` gives `70\t5\tscripts/06_train_and_predict.py` and `141\t0\ttests/test_models_smoke.py`, matching the dispatch and summary; `grep -c '^def test_' tests/test_models_smoke.py` gives exactly **56**, matching.
- **Overclaim sweep**: no acceptance row (WS-14/15, TA-12/13/26) is claimed discharged; the change record's proposed-D-number text remains labelled "NOT a decision"; `evidence/DECISIONS.md` still untouched; no new commit atop `da6cb7b` (`git log -1` reconfirmed). The one factual slip found is finding #1 above (a stale/miscalculated line-count in the summary's own prose), not a claim about behavior or governance.

### Coverage limits

- Did not re-verify the untouched remainder of `scripts/06_train_and_predict.py` beyond the diffed region and the call graph into it; relied on `git diff` to scope the change precisely, per the dispatch.
- Did not exercise the guard's `loaded is None` branch (a loader returning nothing) with a dedicated test-run trace — present in the code and plausible by inspection, but neither iteration's test suite exercises it directly.
- Same read-scope, environment and installability limits as iteration 1 (no scikit-learn/tensorflow/pyyaml; no sibling-unit reads; whole-suite run avoided).

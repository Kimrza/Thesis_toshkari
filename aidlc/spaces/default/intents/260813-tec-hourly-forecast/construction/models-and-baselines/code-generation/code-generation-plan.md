# Code Generation Plan — `models-and-baselines`

**Unit** `models-and-baselines` (Bolt 8) · **Kind** `library` · **Stage** `code-generation`
**Inputs**: `functional-design/` (W-1…W-12; R-90…R-102, R-102a; `domain-entities.md` § 1–13), `nfr-requirements/` (SEC-M-01…SEC-M-06, TS-M-01…TS-M-06), `nfr-design/security-design.md` (SD-M-00…SD-M-07), `nfr-design/logical-components.md` (C-1…C-4), `unit-of-work.md` § 8, `unit-of-work-story-map.md` (WS-14, WS-15, TA-12, TA-13, TA-26), `requirements.md` (FR-P1-04-14, FR-P1-05-1…6, -21, -22), `application-design/component-methods.md` (`Prediction`, `fit_predict`, `three_seed_mean` with `expected_seeds`; § Depth intra-package carve-out), `services.md` (`06`'s writes). Consumed code: `src/features/build.py` (`FeatureBundle`, `FrameSpec`), `src/features/transforms.py` (`Transform`, `transform_id`), `src/data/splits.py` (`Partition`, `build_partitions`, `materialise_locked_partition`), `src/data/config.py` (`ConfigSnapshot`, exception hierarchy incl. `SeedError`, `AlignmentError`, `PartitionError`, `LeakageError`, `LockedTestError`), `src/data/experiment_registry.py`.
**Answers (receipted)**: Q1 = A (**BLK-03 contract approved**), Q2 = B superseded by **FU-1 = C** (Keras code behind a pin guard; pin stays `TBD — freeze gate`), Q3 = A (**`scikit-learn==1.4.2` pinned**), Q4 = B superseded by **FU-2 = B** (owner reopens D-27 by a new D-number FIRST; inverse step gated on it), Q5 = A (**D-121 grids + §8.6 settings transcribed**).
**Authority**: G-09 signed (D-31); BLK-04/BLK-09 approved 2026-09-05 (`CHANGE_RECORD_2026-09-05_R74_R83_leakage_contracts.md`); Q1 = A lifts BLK-03's implementation bar once Step 1's record exists.

## Ground rules binding every step

Same as prior units (3.11 target; in-place edits; no scientific constant in source — seeds, grids, the seven LSTM settings and the ablation parameters reach code ONLY from `configs/`; two-tier errors; module docstrings; ruff clean; a negative control per hard rule; nothing discharged; smoke ≠ governed; **no git commit**). Plus this unit's own: `src/models` never imports `src/external/iri.py`, `src/external/gim.py`, `src/evaluation`, or PyTorch; no residual or GRU module exists (their absence is tested); tuning reads January–November only and the attestation is unconditional (SD-M-01, Q1 = C at nfr-design); no seed is ever selected; RF importance is a diagnostic figure that never feeds selection; the +24 h horizon is config-only; **no `DEC` execution** — the one-shot write path exists and stops at `materialise_locked_partition`'s G-05 signature guard; `tensorflow` is imported only inside the pin-guarded path and never at module scope; `scikit-learn` is imported lazily and its absence refuses by name; **no TBD sentinel is filled** — the transcription in Step 2 copies approved decisions under their D-numbers and nothing else.

## Recorded input — nfr-design review (human ruling 2026-09-05)

This unit's terminal READY (2026-09-04 iteration 2, re-affirmed 2026-09-05) carries **no Minor findings**. The earlier passes' Majors — the Q2 = A attestation field group missing from `domain-entities.md` § 5, the D-121/D-122 attribution, the D-122 status quote — were fixed before the terminal verdict. Nothing is record-only; the `TuningRecord` built in Step 6 carries the seven approved fields PLUS the three attestation fields (`attested_by`, `attested_at_utc`, `attests_criterion_hash`) as `security-design.md` § SD-M-01 specifies, and the plan states this so the field-count history is not reopened by accident.

## Steps

- [ ] **Step 1 — Change record FIRST: `governance/CHANGE_RECORD_2026-09-06_BLK03_confirmatory_contract.md`** [Q1 = A; Q3 = A; Q5 = A; FU-1 = C; FU-2 = B]
  Records the owner's approval of the confirmatory-prediction contract (`domain-entities.md` § 3's four limbs, the five-point consumption contract, R-91/R-92/R-93) as the governed BLK-03 cross-unit contract; the `scikit-learn==1.4.2` pin as an engineering pin on foundation's precedent; the D-121 / Vision §8.6 transcription as a copy under an approved D-number; the FU-1 = C posture (Keras code behind a pin guard, pin unfrozen, deviation from nothing); and FU-2 = B with a **proposed D-number text** the owner may adopt or edit in `evidence/DECISIONS.md` (reopening D-27's inverse withholding: the new argument, D-27's reasoning honoured, the `Transform.inverse(frame)` naming per `component-methods.md`, the import graph unchanged). States plainly that Step 7 is gated on that D-number existing on disk. Blocker-register annotations (BLK-03 contract approved) routed to the gate.

- [ ] **Step 2 — Governed pins and transcription: `requirements.txt`, `configs/experiment.yaml`** [Q3 = A; Q5 = A; R-96; TS-M-01/TS-M-03]
  `requirements.txt`: add `scikit-learn==1.4.2` under the scientific-base block with a comment citing this unit's change record; TensorFlow stays a comment-only `TBD — freeze gate`. `configs/experiment.yaml`: replace `grids: "TBD — freeze gate"` with the exact D-121 / Vision §8.6 grids (Ridge `alpha` six values; RF `n_estimators` × `max_depth` × `min_samples_leaf` with `max_features: sqrt`, eighteen combinations; LSTM `layers` × `units` × `learning_rate` × `batch_size`, sixteen), each block citing `D-121` and `Vision §8.6`; add `models.lstm_fixed_settings` with the seven §8.6 settings (dropout 0.2, Adam, MSE, max epochs 100, early-stopping patience 10 on validation RMSE, min-improvement 1e-4 TECU, best-checkpoint restore) citing §8.6; add `ablations` as the five TE §7.2 named entries (identities and Phase-1 reachability only; any parameter §7.2 does not fix stays `TBD — freeze gate`); every other field untouched. **Nothing that D-121 / §8.6 / §7.2 does not fix is written.**

- [ ] **Step 3 — `src/models/persistence.py`, `src/models/climatology.py` (new)** [FR-P1-05-1; R-98, R-102; W-2, W-9; `domain-entities.md` § 1, § 6]
  M-01 persistence and M-02 24-hour seasonal persistence (no fitted state); M-03 station×month×hour climatology **fitted on training partitions only** with a `FittedPartitionRecord` naming the partitions read (a validation or `DEC` row in the fitting input raises `LeakageError`). All three return the approved `Prediction` shape with `partition_id` / `transform_id` copied from the bundle.

- [ ] **Step 4 — `src/models/ridge.py`, `src/models/random_forest.py` (new)** [FR-P1-05-1; R-96, R-100; TS-M-03; `domain-entities.md` § 7, § 10]
  M-04 Ridge and M-05 Random Forest (direct only) over the config grids; `scikit-learn` imported **lazily** inside the fit path and its absence refuses naming the `requirements.txt` pin; `scikit-learn` splitters never used; grid search bounded by config CONTENT (a grid value not in config refuses); RF importance emitted only as an `ImportanceFigure` carrying the diagnostic-only marker and never returned on any selection path.

- [ ] **Step 5 — `src/models/checkpoint.py`, `src/models/lstm.py` (new)** [FR-P1-05-3, FR-P1-05-5; R-94, R-96; W-4; TS-M-01/02/04; FU-1 = C; `domain-entities.md` § 4, § 7]
  `checkpoint.py`: backend-neutral `Checkpoint` selection on **lowest validation RMSE** over a recorded epoch history, `restore` returning that checkpoint (a last-epoch restore fails), serialization contract expressed as an interface the backend fills. `lstm.py`: M-06 compact LSTM against the tf.keras 2.21.0 candidate API, **every `tensorflow` import inside `_require_frozen_pin()`-guarded code** that refuses (naming TS-M-01 and the pin) unless `requirements.txt` carries a frozen `tensorflow==` line; grid enumeration (sixteen) and the seven fixed settings asserted against config, never held in source; determinism utility (`set_random_seed`, op determinism "where supported") and `nondeterministic_ops` recorded as a measured output, not a list.

- [ ] **Step 6 — `src/models/train.py` (new)** [FR-P1-04-14, FR-P1-05-2, -4, -6, -21, -22; R-90…R-93, R-95, R-96, R-97, R-99, R-101, R-102; W-1, W-3, W-5, W-6, W-7, W-8; SD-M-01, SD-M-02, SD-M-03; `domain-entities.md` § 2, § 3, § 5, § 8, § 9, § 11]
  `fit_predict(model_id, *, bundle, partition, snapshot)` (approved signature; `LeakageError` when `bundle.transform_id is None`; closed model set — an id outside M-01…M-06 refuses); `assert_stamp_match(bundle, partition_being_scored)` as the named R-90 function with the three checks (`PartitionError` × 2, `LeakageError`); `three_seed_mean(predictions, *, expected_seeds)` enforcing all four limbs (`SeedError` incl. the wrong-but-distinct triple, `AlignmentError` on the ordered (`station`, `interval_start_utc`) index as a set AND in order, `PartitionError`, `LeakageError`) with `expected_seeds` read from `ConfigSnapshot.seeds` at the call site and never inlined; `tune(...)` with `TuningRecord` (seven approved fields + three attestation fields), partitions restricted to January–November, `criterion_hash == criterion_used_hash`, **unconditional attestation** (SD-M-01, Q1 = C); grid freeze as content + immutability (R-96: content asserted against config, grid hash committed); `select(...)` on mean per-fold skill with the refit changing no hyperparameter (R-101); ablation registry of the five named entries read from `experiment.yaml` (R-97) with `ABL-HIST48` refusing until the primary configuration is frozen and `ABL-DIFF` refusing while no inverse exists (naming D-27) — see Step 7; `HorizonSpec` config-only (R-99).

- [ ] **Step 7 — GATED: `Transform.inverse` in `src/features/transforms.py` and `ABL-DIFF` runnable** [FU-2 = B; D-27; R-84 / R-103 naming]
  **Precondition, checked on disk when this step is reached**: `evidence/DECISIONS.md` carries a new D-number (dated on or after 2026-09-06) that explicitly reopens D-27's inverse withholding, states its new argument, honours D-27's reasoning, and settles the naming. **If present**: add `Transform.inverse(frame) -> frame` to `src/features/transforms.py` (features-and-splits' module, edited in place, additive only, flagged for that unit's record and re-check), keep the import graph unchanged (no `src/evaluation` → `src/features` import; the method travels with `transform_id`), make `ABL-DIFF` inverse-transform to absolute TECU before any metric, and add the inverse's negative controls (inverse of an untransformed frame refuses; inverse under a mismatched `transform_id` refuses). **If absent**: STOP at this step, tick nothing, report the absence in the return summary, and leave `ABL-DIFF` refusing exactly as Step 6 built it. Nothing else in this plan waits on this step.

- [ ] **Step 8 — `scripts/06_train_and_predict.py` (new)** [W-12; R-102a; SD-M-04; §12/§13.2; `domain-entities.md` § 13]
  Position 06; six-step entry (`ensure_process_determinism` first; `assert_no_raw_fields` before first write); reads bundles by manifest; `assert_stamp_match` before EVERY scoring path; the three-seed run per fitting-capable partition; `--partition` choices exclude `DEC` from the default list; the one-shot `DEC` write path implemented in full — write once, `sha256` as written, `PredictionHashReceipt` (`prediction_path`, `sha256`, `recorded_at_utc`, `run_id`, `partition_id`) via `.tmp` → `fsync` → atomic rename → registry append at column 18 → **refuse to exit** (`LockedTestError`) unless rename AND append both succeeded — and **unreachable today** because it enters through `materialise_locked_partition(snapshot, g05_signature=...)`, which refuses without a verified G-05 signature; honest `aborted` registry row on every `IntegrityError`; `prior_period_exposure` never written here.

- [ ] **Step 9 — Tests** [§12-mandated `tests/test_models_smoke.py`, `tests/test_checkpoint_restore.py`]
  `test_models_smoke.py`: closed model set (M-07 refuses; residual/GRU/PyTorch absence by identifier scan over `src/models`); M-01/M-02/M-03 happy path over synthetic frames; M-03 training-only control; M-04/M-05 refusal-by-name without `scikit-learn` and grid-content controls against config (6 / 18 / 16 re-read from `experiment.yaml`, a value outside the grid refuses); the four R-90 stamp-match controls with control 3 by enumeration over the six ids and the must-not-fire control; `three_seed_mean` controls — single seed, best-of-three, wrong-but-distinct `{1338, 2024, 7}` (constructed from the config set at test time, never as a literal set of the real seeds), `seed is None` input, non-M-06 input, one-row misalignment, same rows different order, cross-partition, training partition, `transform_id` disagreement / `None`, and the pass case with provenance copied and `seed is None`; tuning: December partition in `partitions_read` refuses, `criterion_hash != criterion_used_hash` refuses, missing attestation refuses; ablations registered from config, `ABL-HIST48` refuses before the freeze, `ABL-DIFF` refuses without the inverse (or runs, if Step 7 landed); horizon config-only; RF importance marker; M-06 pin guard refuses while no frozen `tensorflow==` pin exists and the seven settings are asserted from config; `06`'s receipt controls (no receipt → raises in `06`; hash mismatch → raises in `06`; unflushed/failed append → raises in `06`) driven through the script's functions with a synthetic non-`DEC` fixture and the `DEC` guard asserted to refuse. `test_checkpoint_restore.py`: lowest-validation-RMSE selection over a recorded history, restore returns that checkpoint, last-epoch restore fails, tie and NaN handling stated, fake-backend round trip. No test touches December 2022 content, the restricted root, or a real signature; no seed, grid or setting value appears as a literal in any test.

- [ ] **Step 10 — Full-suite smoke + lint** — every module `py_compile`s; the owned modules and the whole suite run under the available interpreter (stdlib stand-in if the pins are still not installable — smoke only, never governed); ruff clean on touched files if ruff is available, otherwise the stdlib line-length / unused-import substitute with ruff recorded as owed.

- [ ] **Step 11 — Governance stop before commit (student acts)**
  Step 1's change record exists FIRST. Gate items: blocker-register annotation (BLK-03 contract approved; evidence limb open at G-05); the owner's D-number reopening D-27 (adopted or not — Step 7's outcome); the TensorFlow pin (unfrozen; M-06's Keras path written but unexecutable; TA-26 `Pending`); the `scikit-learn` pin's install evidence owed; the `pyarrow` pin carried from features-and-splits; the `requirements.md` FR-P1-05-2 bootstrap-seed attribution disagreement (raised by `domain-entities.md` § 3, not edited); the `prior_period_exposure` deviation from the approved remediation text (R-102a); commit citing **D-121, D-122, D-27, D-31** as touched context plus the new D-number if adopted. **No governed commit before the records exist.** Nothing discharged: WS-14, WS-15, TA-12, TA-13, TA-26 stay `Pending`; the seven rowless requirements stay rowless.

## Out of scope

Training any model on real data (no released features exist; the permitted-producer list is unset); freezing the TensorFlow pin; executing `DEC`; writing `prior_period_exposure`; `07_evaluate_and_report.py` and `test_common_masks.py` (evaluation-and-comparison's); the bootstrap (statistical-inference's); any acceptance-row discharge.

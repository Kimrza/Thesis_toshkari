# Practices Discovery — Stage Memory

Observation diary for stage 2.2 (practices-discovery). Maintained while the
stage runs; read by the §13 learnings ritual before the approval gate.

## Interpretations

- 2026-08-15T20:20:34Z — Treated the run as brownfield-with-thin-evidence; reverse-engineering is SKIP in this scope, so the lead draft works from the workspace itself (scripts/, notebooks/, evidence/, governance/, PreFlight/) plus org.md defaults rather than reverse-engineering artifacts. The recovery protocol's "producer is SKIP → absent by design" rule covers the missing code-structure/technology-stack inputs.

- 2026-08-16T00:00:00Z — Two of the lead draft's open questions were closed by the support reviews rather than by the human: the active scope file declares `skeleton: off`, and §13.1/§13.4/TA-01 require a per-run code commit, so version control is fixed by the normative core. Reframed both as narrower questions (how to work inside git; confirm the independently-binding §9.2 fixtures) rather than deleting them, since the human's disposition still shapes the practice.

## Deviations

- 2026-08-16T00:00:00Z — Presented the support reviewers' Issues sections to the human before the interview, per stage-protocol §11, rather than folding them silently into the questions. Three of the fifteen questions (Q13 identity/PII, Q14 integrity gaps, Q15 document count contradiction) exist only because the reviewers ran the checks the normative core mandates instead of reading its policy sentences; the lead draft had asserted compliance it had not measured.

## Tradeoffs

- 2026-08-16T00:00:00Z — Wrote 15 interview questions against a Comprehensive depth guide of ~8-12+. Considered folding the reviewer-surfaced remediation items (Q13-Q15) into a single "how to handle the gaps" question; kept them separate because each has a different owner (student alone, student+supervisor, supervisor countersignature) and merging them would have hidden that split, against the project rule requiring verification obligations and governance dependencies to be kept apart.

## Open questions

- 2026-08-16T00:00:00Z — The TEC governance overlay review could not complete: five adaptive-board seats were dispatched and all five were terminated mid-pass by a session limit. A single retried Chair pass completed and returned FAIL with three blockers, plus a full-board escalation trigger (protocol drift, target-lineage ambiguity) that has not been run. Under the overlay's own rules an incomplete board is NOT REVIEWABLE, so the stage's affirmation gate was not presented and no promotion into memory/ occurred. The §13 learnings ritual has NOT yet run for this stage.
- 2026-08-16T00:00:00Z — CORRECTED FINDING (supersedes the first reading, which was wrong and was fed to the Chair seat as established fact; CHAIR-03 must be re-scoped against this text). Verified across all twelve month folders by source-file scan. Every folder carries exactly one preceding-day straddle file, which is documented behaviour for UTC day boundaries in `scripts/merge_coverage_year.py`. Two folders carry an extra file that is not a straddle: `evidence/audit_evidence_2022-01/` contains `gps221231g.003.hdf5` (31 December 2022, the LOCKED-TEST month, 743 ARUC records) alongside its correct `gps211231g.002.hdf5` straddle; `evidence/audit_evidence_2022-12/` contains `gps211231g.002.hdf5` (31 December 2021, 642 records) alongside its correct `gps221130` straddle. One root cause: the 31-December lookup in the acquisition query is not year-scoped, so it matched both years and filed each in the wrong folder. Consequence: `audit_evidence_2022-01/madrigal_coverage_summary.csv` is wrong — its `unique_days = 32` counts a locked-test-month day, and it reports `december_days_present = 1, december_coverage_pct = 3.226` from a January run.
- 2026-08-16T00:00:00Z — WITHDRAWN claims from the first reading, recorded so the board does not re-inherit them: (a) the out-of-window rows do NOT corrupt `audit_evidence_2022-FULL/` — the merge script's documented year guard excludes them from statistics while keeping them in merged raw records for evidence completeness, and FULL correctly reports 365 days / 100% for all three stations; (b) `unique_days = 32` in the December folder is NOT an impossible value — it is 2022-11-30 (straddle) plus 31 December days, with the 2021 date already excluded by the guard; (c) the `month 11` row is NOT phantom — it is that same documented straddle, reported honestly. The inference built on (a)-(c), that the hash chain cannot detect wrong-window or wrong-math errors, is therefore unsupported and must not be carried into the full-board review.
- 2026-08-16T00:00:00Z — Full board completed on the corrected facts: seven seats (Chair, TEC, ML/Statistics, Data/Reproducibility, Benchmark, Validation Auditor, Implementation). Consolidated verdict FAIL. The Implementation seat exercised its veto on two exclusive-domain conditions (missing critical tests; protected Phase 1 protocol drift). The Validation Auditor considered and REJECTED treating the January locked-month retrieval as unauthorized access, on manifest timestamps showing the authorized Vision §8.3 December coverage audit ran ~12 hours earlier — classified a within-authorization irregularity. Root cause of the misfiling located by the Implementation seat: `notebooks/madrigal_phase1_coverage_audit.ipynb` cell 10, predicate `if exp.startmonth not in RUN_MONTHS and exp.endmonth not in RUN_MONTHS: continue` tests month without year; one-line fix is a `(year, month)` membership test.
- 2026-08-16T00:00:00Z — Human decision: hold all remediation for the full seven-seat board rather than fixing in this stage. No code change, no re-derivation, and no edit to the four stage artifacts was made. The evidence base is untouched.
- 2026-08-16T00:00:00Z — Chair finding to carry regardless of how the board resolves: `team-practices.md` states both fixture windows are "fixed now as a D-number entry", but `evidence/DECISIONS.md` holds D-1..D-10 only and no fixture entry exists. Vision §1.2 and TE §1.1 forbid a coding agent filling a `TBD — freeze gate` value by convenience; the wording promotes an unresolved Q-31 as settled.

- 2026-08-15T20:20:34Z — The workspace is not a git repository, while org.md's Way of Working assumes trunk-based development with feature branches and squash-merged Bolt branches. Whether the team adopts git for this thesis project is a human decision the interview must settle, not something evidence can answer.
